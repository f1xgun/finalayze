---
phase: 32-critical-sandbox-fixes
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-04-07T17:50:01.642Z
---

<current_state>
Wave 1 (Plans 32-01 + 32-03) COMPLETE. Wave 2 (Plan 32-02) NOT STARTED.

Plans 32-01 and 32-03 ran in parallel (Wave 1). Plan 32-02 depends on 32-01
because both modify trading_loop.py. Wave 1 changes are committed and tested.
</current_state>

<completed_work>

## Plan 32-01: Data Pipeline & Safety Defaults (2 tasks, DONE)
- Task 1: `_CANDLE_LOOKBACK` 60→210, kill switch startup check — commit `2760f02`
- Task 2: Sandbox rollout default MINIMAL, staleness 72h + MOEX holiday awareness — commit `ae22dfb`

## Plan 32-03: ML Quality Gate Fixes (1 task, DONE)
- Task 1: profit_factor computed from fold predictions, calibrator parameter added — commit `a8d861e`
- Tests: 7 new tests in `tests/unit/test_ml_quality_gates.py`, all passing

</completed_work>

<remaining_work>

## Plan 32-02: Sandbox Wiring + News + Diagnostics (2 tasks, NOT STARTED)

### Task 1: CachingFetcher + RateLimiter wiring
- Wrap TinkoffFetcher in CachingFetcher in `scripts/run_sandbox.py`
- Pass RateLimiter (4 req/sec) to TinkoffFetcher in sandbox
- Same wiring in `src/finalayze/main.py` `_build_trading_loop()`
- Requirements: SANDBOX-FIX-05, SANDBOX-FIX-06

### Task 2: News pipeline activation + signal diagnostics
- Enable `event_driven: true` in `ru_blue_chips.yaml`, `ru_energy.yaml`, `ru_finance.yaml`
- Document LLM setup in .env.example
- Add per-gate counters to CycleLogEntry: signals_dropped_no_bars, signals_dropped_below_threshold, signals_dropped_pre_trade
- Increment counters in _process_instrument and _process_market_cycle
- Requirements: SANDBOX-FIX-07, SANDBOX-FIX-08

</remaining_work>

<decisions_made>

- MOEX-first: all fixes target ru_* segments, US is deferred
- v7.0 milestone created: Agent Intelligence & Experiment Framework (Phases 32-35)
- Structured Debate Protocol (Phase 33) will enforce evidence-based agent outputs
- Experiment Framework (Phase 34) will A/B test conflicting agent recommendations
- ML calibrator parameter added to _evaluate_fold_metrics but not wired in walk-forward loop (per-fold calibrator doesn't exist yet — future phase)

</decisions_made>

<blockers>
None — all dependencies satisfied for Plan 32-02.
</blockers>

<context>
This session started with a comprehensive "board of directors" meeting using 8 expert agents
across 2 rounds. The meeting identified why the sandbox generates very few signals:

1. _CANDLE_LOOKBACK=60 kills 3 of 6 strategies (FIXED in Wave 1)
2. ML is functionally dead — all weights 0.0 (FIXED gate bugs in Wave 1)
3. News pipeline fully wired but no LLM key set (to be enabled in Wave 2)
4. 14 gates in signal path with cascading filters
5. Staleness check blocks Monday mornings (FIXED in Wave 1)

The user wants to focus on MOEX first, then build a meta-system for agent quality
control (Phases 33-35) before expanding to other markets.
</context>

<next_action>
Start with: Read 32-02-PLAN.md, then execute Task 1 (CachingFetcher + RateLimiter wiring
in scripts/run_sandbox.py and src/finalayze/main.py). After that, Task 2 (enable event_driven
presets + add signal diagnostics). Then create 32-02-SUMMARY.md and run phase verification.
</next_action>

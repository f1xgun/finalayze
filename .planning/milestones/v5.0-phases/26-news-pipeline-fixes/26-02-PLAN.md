---
phase: 26-news-pipeline-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/analysis/entity_extractor.py
  - src/finalayze/data/fetchers/telegram_reader.py
  - tests/unit/test_entity_extractor.py
  - tests/unit/test_telegram_reader.py
autonomous: true
requirements: [NEWS-03, NEWS-04]

must_haves:
  truths:
    - "Entity extractor _VALID_TICKERS contains TCSG and does not contain bare T -- news about T-Bank resolves to TCSG"
    - "TelegramChannelReader skips messages whose URL was already processed within the dedup window"
  artifacts:
    - path: "src/finalayze/analysis/entity_extractor.py"
      provides: "Corrected _VALID_TICKERS frozenset"
      contains: "TCSG"
    - path: "src/finalayze/data/fetchers/telegram_reader.py"
      provides: "URL-based message deduplication"
      contains: "_seen_urls"
    - path: "tests/unit/test_entity_extractor.py"
      provides: "Test TCSG in valid tickers, T not in valid tickers"
    - path: "tests/unit/test_telegram_reader.py"
      provides: "Dedup tests"
  key_links:
    - from: "entity_extractor._VALID_TICKERS"
      to: "LLM ticker output"
      via: "frozenset membership check"
      pattern: '"TCSG" in _VALID_TICKERS'
    - from: "telegram_reader._seen_urls"
      to: "_parse_message"
      via: "URL dedup check before appending"
      pattern: "_seen_urls.*msg_url"
---

<objective>
Fix TCSG ticker mismatch in entity extractor and add URL-based message deduplication to Telegram reader.

Purpose: Prevent T-Bank news from being silently dropped (wrong ticker "T") and stop duplicate Telegram messages from burning LLM tokens.
Output: Fixed entity_extractor.py and telegram_reader.py with dedup, plus tests.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-news-pipeline-fixes/26-CONTEXT.md

@src/finalayze/analysis/entity_extractor.py
@src/finalayze/data/fetchers/telegram_reader.py
@src/finalayze/data/fetchers/rss_fetcher.py

<interfaces>
<!-- From entity_extractor.py line 30-62 -->
_VALID_TICKERS: frozenset[str] = frozenset({
    "SBER", "GAZP", "LKOH", "YDEX", "ROSN", "GMKN", "MGNT", "VTBR",
    "MTSS", "PLZL", "NVTK", "SNGS", "TATN", "CHMF", "NLMK", "MAGN",
    "ALRS", "IRAO", "RUAL", "MOEX",
    "T",      # <-- BUG: should be "TCSG"
    "OZON", "PIKK", "AFKS", "TRNFP", "AFLT", "MSNG", "HYDR", "PHOR",
})

<!-- From rss_fetcher.py — dedup pattern to follow -->
from collections import OrderedDict
_MAX_SEEN_SIZE = 5000

class RssNewsFetcher:
    def __init__(self, ...):
        self._seen_urls: OrderedDict[str, None] = OrderedDict()

    def _parse_entry(self, entry) -> NewsArticle | None:
        link = getattr(entry, "link", "")
        if link in self._seen_urls:
            return None
        self._seen_urls[link] = None
        if len(self._seen_urls) > self._MAX_SEEN_SIZE:
            self._seen_urls.popitem(last=False)
        ...

<!-- From telegram_reader.py -->
class TelegramChannelReader:
    def __init__(self, *, channels: list[str] | None = None):
        self._channels = channels or []
        # NO dedup mechanism currently

    def _parse_message(self, widget, channel, cutoff) -> NewsArticle | None:
        # Extracts msg_url from data-post attribute
        # Returns NewsArticle with url=msg_url
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix TCSG ticker and add Telegram message deduplication</name>
  <files>
    src/finalayze/analysis/entity_extractor.py,
    src/finalayze/data/fetchers/telegram_reader.py,
    tests/unit/test_entity_extractor.py,
    tests/unit/test_telegram_reader.py
  </files>
  <behavior>
    - Test: "TCSG" is in _VALID_TICKERS
    - Test: "T" is NOT in _VALID_TICKERS
    - Test: EntityExtractor.extract() keeps "TCSG" in output when LLM returns it
    - Test: EntityExtractor.extract() filters out "T" (not in valid set)
    - Test: TelegramChannelReader does not return the same message URL twice across consecutive fetch_recent_messages calls
    - Test: TelegramChannelReader returns a message on first fetch, skips it on second fetch
    - Test: Dedup seen set does not grow beyond _MAX_SEEN_SIZE (5000) -- oldest entries evicted
    - Test: Different message URLs are returned normally (dedup does not over-filter)
  </behavior>
  <action>
    **NEWS-03: Fix TCSG ticker**

    1. In `src/finalayze/analysis/entity_extractor.py` line 52, replace `"T"` with `"TCSG"` in the _VALID_TICKERS frozenset. This is the MOEX ticker for T-Bank (formerly Tinkoff). The bare "T" would match AT&T on NYSE, not T-Bank on MOEX.

    **NEWS-04: Telegram message deduplication**

    2. In `src/finalayze/data/fetchers/telegram_reader.py`:

       a. Add import: `from collections import OrderedDict`

       b. Add module constant: `_MAX_SEEN_SIZE = 5000`

       c. In `__init__`, add: `self._seen_urls: OrderedDict[str, None] = OrderedDict()`

       d. In `_parse_message()`, after computing `msg_url` (around line 157) and before the final `return NewsArticle(...)`, add dedup check:
          ```python
          if msg_url in self._seen_urls:
              return None
          self._seen_urls[msg_url] = None
          if len(self._seen_urls) > _MAX_SEEN_SIZE:
              self._seen_urls.popitem(last=False)
          ```

       This follows the exact same pattern as RssNewsFetcher._parse_entry() for consistency.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_entity_extractor.py tests/unit/test_telegram_reader.py -x -q --tb=short -k "tcsg or valid_ticker or dedup"</automated>
  </verify>
  <done>
    - _VALID_TICKERS contains "TCSG", does not contain "T"
    - TelegramChannelReader skips messages with previously-seen URLs
    - Seen URL set bounded at 5000 entries with LRU eviction
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_entity_extractor.py tests/unit/test_telegram_reader.py -x -q --tb=short
```
</verification>

<success_criteria>
- "TCSG" in _VALID_TICKERS and "T" not in _VALID_TICKERS
- Duplicate Telegram messages (same URL) are filtered on second fetch
- Seen URL set bounded at 5000
- All pre-existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-news-pipeline-fixes/26-02-SUMMARY.md`
</output>

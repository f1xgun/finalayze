---
phase: 22-dependency-layer-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/finalayze/orchestration/trading_loop.py
  - src/finalayze/backtest/CLAUDE.md
  - src/finalayze/monitoring/CLAUDE.md
autonomous: true
requirements:
  - LAYER-03
  - LAYER-04

must_haves:
  truths:
    - "TradingLoop accepts an optional MetricsCollector via constructor parameter"
    - "TradingLoop has no deferred import of finalayze.api.metrics inside method bodies"
    - "backtest/ CLAUDE.md documents its layer assignment"
    - "monitoring/ has a CLAUDE.md documenting Layer 6 assignment"
  artifacts:
    - path: "src/finalayze/orchestration/trading_loop.py"
      provides: "TradingLoop with injected MetricsCollector"
      contains: "metrics_collector"
    - path: "src/finalayze/monitoring/CLAUDE.md"
      provides: "Layer documentation for monitoring module"
    - path: "src/finalayze/backtest/CLAUDE.md"
      provides: "Updated layer documentation for backtest"
  key_links:
    - from: "src/finalayze/main.py"
      to: "src/finalayze/orchestration/trading_loop.py"
      via: "MetricsCollector passed to TradingLoop constructor"
      pattern: "TradingLoop\\(.*metrics_collector"
---

<objective>
Inject MetricsCollector into TradingLoop via constructor and document layer assignments for backtest/ and monitoring/.

Purpose: LAYER-03 eliminates TradingLoop's direct dependency on api.metrics (L6 importing into what is now L5 orchestration). LAYER-04 ensures all modules have documented layer assignments for maintainability.

Output: TradingLoop accepts Optional[MetricsCollector] constructor parameter. All 6 deferred MetricsCollector imports inside trading_loop.py method bodies are replaced with `self._metrics` attribute usage. backtest/ and monitoring/ have documented layers.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dependency-layer-cleanup/22-CONTEXT.md
@.planning/phases/22-dependency-layer-cleanup/22-01-SUMMARY.md
</context>

<interfaces>
<!-- MetricsCollector interface from src/finalayze/api/metrics.py -->
<!-- Executor needs to know what MetricsCollector looks like to wire it -->
From src/finalayze/api/metrics.py:
```python
class MetricsCollector:
    # All static methods — no instance state needed
    @staticmethod
    def set_portfolio_equity(market: str, value: float) -> None: ...
    @staticmethod
    def record_trade(market: str, strategy: str, ...) -> None: ...
    # etc. — all static methods
```

Current deferred imports in trading_loop.py (6 sites):
- Line ~541: `from finalayze.api.metrics import MetricsCollector`
- Line ~1207: `from finalayze.api.metrics import MetricsCollector`
- Line ~1348: `from finalayze.api.metrics import MetricsCollector`
- Line ~1558: `from finalayze.api.metrics import MetricsCollector`
- Line ~1591: `from finalayze.api.metrics import MetricsCollector`
- Line ~1847: `from finalayze.api.metrics import MetricsCollector`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inject MetricsCollector into TradingLoop constructor</name>
  <read_first>
    - src/finalayze/orchestration/trading_loop.py (full file — need to find all 6 MetricsCollector import sites and __init__ signature)
    - src/finalayze/api/metrics.py (first 30 lines — understand MetricsCollector interface)
    - src/finalayze/main.py (lines around 220-230 — where TradingLoop is constructed)
  </read_first>
  <files>
    src/finalayze/orchestration/trading_loop.py
    src/finalayze/main.py
  </files>
  <action>
    1. **Add constructor parameter**: In TradingLoop.__init__, add `metrics_collector: type | None = None` parameter. Store as `self._metrics = metrics_collector`. Use `type` (not an instance) because MetricsCollector uses all static methods.

    2. **Add TYPE_CHECKING import** at top of trading_loop.py:
       ```python
       if TYPE_CHECKING:
           from finalayze.api.metrics import MetricsCollector
       ```

    3. **Replace all 6 deferred imports** of MetricsCollector inside method bodies. For each site:
       - Remove the `from finalayze.api.metrics import MetricsCollector` line
       - Replace `MetricsCollector.some_method(...)` with `if self._metrics: self._metrics.some_method(...)`
       - This makes MetricsCollector truly optional — when None, metrics are silently skipped

    The 6 sites to update (line numbers may shift after Plan 01 moves):
       - Search for `from finalayze.api.metrics import MetricsCollector` — there should be exactly 6 occurrences
       - Each one is inside a method body, followed by a `MetricsCollector.` call
       - Wrap each call in `if self._metrics:` guard

    4. **Wire in main.py**: Where TradingLoop is constructed (around line 221), pass MetricsCollector:
       ```python
       from finalayze.api.metrics import MetricsCollector
       loop = TradingLoop(..., metrics_collector=MetricsCollector)
       ```

    5. **Verify no remaining api.metrics imports in trading_loop.py**:
       `grep "from finalayze.api.metrics" src/finalayze/orchestration/trading_loop.py` should return empty (only TYPE_CHECKING guard remains).
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run python -c "from finalayze.orchestration.trading_loop import TradingLoop; print('OK')" && ! grep -q "from finalayze.api.metrics import MetricsCollector" src/finalayze/orchestration/trading_loop.py || (echo 'FAIL: still has deferred import' && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - TradingLoop.__init__ accepts `metrics_collector` parameter
    - No `from finalayze.api.metrics import MetricsCollector` inside method bodies
    - main.py passes MetricsCollector to TradingLoop constructor
    - All tests pass (MetricsCollector=None path works for tests that don't provide it)
  </acceptance_criteria>
  <done>TradingLoop has no direct import of api.metrics. MetricsCollector is injected via constructor. Metrics collection still works in production (main.py wires it).</done>
</task>

<task type="auto">
  <name>Task 2: Document layer assignments for backtest/ and monitoring/</name>
  <read_first>
    - src/finalayze/backtest/CLAUDE.md
    - src/finalayze/monitoring/__init__.py
    - src/finalayze/monitoring/health_monitor.py (first 30 lines)
    - src/finalayze/monitoring/sandbox_monitor.py (first 30 lines)
    - src/finalayze/monitoring/anomaly_detector.py (first 30 lines)
    - src/finalayze/monitoring/go_no_go.py (first 30 lines)
  </read_first>
  <files>
    src/finalayze/backtest/CLAUDE.md
    src/finalayze/monitoring/CLAUDE.md
  </files>
  <action>
    1. **Update backtest/CLAUDE.md**: Change the Layer line from "Layer 4-5 boundary" to a definitive assignment:
       - "Layer: Cross-cutting test infrastructure (Layer 4-5). Imports from strategies (L4), risk (L4), and simulated execution (L5). Does NOT import from API (L6) or monitoring (L6). Bond engine imports apply_ofz_rotation from orchestration (L5)."
       - Add note: "Import rule: backtest/ may import from L0-L5. Must NOT import from L6 (api/, monitoring/, dashboard/)."

    2. **Create monitoring/CLAUDE.md** with full module documentation:
       ```
       # Monitoring

       ## Purpose
       Runtime health monitoring, anomaly detection, sandbox validation, and go/no-go checks for the trading system.

       ## Layer
       Layer 6 — Monitoring. Imports from orchestration (L5), api (L6), and all lower layers. Consumed by api/ endpoints and main.py lifespan.

       ## Key Files
       - health_monitor.py — HealthMonitor: tracks feed freshness, cycle health, component status. Alerts via TelegramAlerter on degradation.
       - sandbox_monitor.py — SandboxMonitorService: monitors sandbox trading performance, persists metrics, validates paper trading results.
       - anomaly_detector.py — AnomalyDetector: statistical anomaly detection on portfolio metrics (drawdown spikes, unusual returns, volume anomalies).
       - go_no_go.py — GoNoGoChecker: pre-production validation checklist (data freshness, model staleness, risk limits, broker connectivity).

       ## Public API
       - HealthMonitor — feed timestamp tracking, cycle health reporting
       - SandboxMonitorService — sandbox performance monitoring
       - AnomalyDetector — anomaly detection and alerting
       - GoNoGoChecker — production readiness validation

       ## Contracts
       - Input: TradingLoop reference (for health checks), TelegramAlerter (for alerts)
       - Output: Health status dicts, anomaly alerts, go/no-go verdicts
       - Import rule: monitoring/ may import from L0-L5 and L6 (api/). Must NOT be imported by L0-L4.

       ## Testing
       - Test location: tests/unit/test_health_monitor.py, tests/unit/test_sandbox_monitor.py
       - Run: uv run pytest tests/unit/test_health_monitor.py -v
       ```

    3. **Verify import analysis**: Run `uv run ruff check src/finalayze/backtest/ src/finalayze/monitoring/` to confirm no layer violations.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && test -f src/finalayze/monitoring/CLAUDE.md && grep -q "Layer 6" src/finalayze/monitoring/CLAUDE.md && grep -q "Cross-cutting" src/finalayze/backtest/CLAUDE.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - monitoring/CLAUDE.md exists with Layer 6 assignment
    - backtest/CLAUDE.md has definitive layer assignment (not vague "boundary")
    - Both documents follow the established CLAUDE.md format (Purpose, Layer, Key Files, Public API, Contracts, Testing)
  </acceptance_criteria>
  <done>backtest/ and monitoring/ have documented, definitive layer assignments confirmed by import analysis.</done>
</task>

</tasks>

<verification>
1. `grep -r "from finalayze.api.metrics import MetricsCollector" src/finalayze/orchestration/trading_loop.py` returns empty
2. `grep "metrics_collector" src/finalayze/orchestration/trading_loop.py` finds constructor parameter
3. `test -f src/finalayze/monitoring/CLAUDE.md` succeeds
4. `uv run pytest tests/ -x -q --timeout=120` — all tests pass
</verification>

<success_criteria>
- TradingLoop has zero deferred imports of MetricsCollector
- MetricsCollector is injected via constructor in main.py
- backtest/CLAUDE.md and monitoring/CLAUDE.md both have clear layer assignments
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-dependency-layer-cleanup/22-02-SUMMARY.md`
</output>

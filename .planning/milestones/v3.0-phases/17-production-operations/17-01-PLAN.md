---
phase: 17-production-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/core/kill_switch.py
  - src/finalayze/monitoring/health_monitor.py
  - config/settings.py
  - tests/unit/test_kill_switch.py
  - tests/unit/test_health_monitor.py
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]

must_haves:
  truths:
    - "KillSwitch.activate() cancels orders, stops scheduler, escalates breakers, sends CRITICAL alert"
    - "KillSwitch sets persistent file flag that blocks restart"
    - "KillSwitch completes all steps within 30 seconds (tested)"
    - "HealthMonitor checks broker, feed freshness, loop alive every 5 minutes"
    - "HealthMonitor alerts on 2 consecutive heartbeat failures"
    - "HealthMonitor resets failure counter on successful check"
    - "Kill switch alert uses AlertPriority.CRITICAL (bypasses queue)"
    - "Health monitor warning uses AlertPriority.IMPORTANT"
  artifacts:
    - path: "src/finalayze/core/kill_switch.py"
      provides: "KillSwitch orchestrator with activate(), is_killed, clear_flag"
      exports: ["KillSwitch", "KillSwitchResult"]
    - path: "src/finalayze/monitoring/health_monitor.py"
      provides: "HealthMonitor with start(), stop(), check_now(), last_result"
      exports: ["HealthMonitor", "HealthCheckResult"]
    - path: "tests/unit/test_kill_switch.py"
      provides: "Kill switch unit tests including timing assertion"
    - path: "tests/unit/test_health_monitor.py"
      provides: "Health monitor unit tests including 2-miss alerting"
  key_links:
    - from: "src/finalayze/core/kill_switch.py"
      to: "execution/broker_router.py"
      via: "constructor injection"
      pattern: "broker_router\\.route\\("
    - from: "src/finalayze/core/kill_switch.py"
      to: "risk/circuit_breaker.py"
      via: "override_level(CircuitLevel.LIQUIDATE)"
      pattern: "override_level.*LIQUIDATE"
    - from: "src/finalayze/core/kill_switch.py"
      to: "core/alerts.py"
      via: "send_alert with CRITICAL priority"
      pattern: "AlertPriority\\.CRITICAL"
    - from: "src/finalayze/monitoring/health_monitor.py"
      to: "core/alerts.py"
      via: "send_alert with IMPORTANT priority on 2 misses"
      pattern: "AlertPriority\\.IMPORTANT"
---

<objective>
Create KillSwitch orchestrator and HealthMonitor service -- the two core domain objects for production operations.

Purpose: KillSwitch provides emergency shutdown (<30s SLA) while HealthMonitor provides proactive failure detection with 5-minute heartbeat. Both are standalone classes with constructor-injected dependencies.

Output: Two production-ready classes with comprehensive tests. Settings extended with telegram_admin_chat_id and kill_switch_flag_path.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-production-operations/17-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/finalayze/risk/circuit_breaker.py:
```python
class CircuitLevel(StrEnum):
    NORMAL = "normal"
    CAUTION = "caution"
    HALTED = "halted"
    LIQUIDATE = "liquidate"

class CircuitBreaker:
    def override_level(self, level: CircuitLevel) -> None: ...
    @property
    def level(self) -> CircuitLevel: ...
```

From src/finalayze/execution/broker_router.py:
```python
class BrokerRouter:
    @property
    def registered_markets(self) -> list[str]: ...
    def route(self, market_id: str) -> BrokerBase: ...
```

From src/finalayze/execution/broker_base.py:
```python
class BrokerBase(ABC):
    # NOTE: cancel_all_orders() does NOT exist
    # Use get_open_orders() + cancel_order() per order
```

From src/finalayze/execution/tinkoff_broker.py:
```python
class TinkoffBroker(BrokerBase):
    def get_open_orders(self) -> list: ...
    def cancel_order_safe(self, order_id: str) -> bool: ...
```

From src/finalayze/core/alerts.py:
```python
class AlertPriority(StrEnum):
    CRITICAL = "critical"    # bypasses queue entirely
    IMPORTANT = "important"  # batched with priority
    INFO = "info"            # batched normally

class TelegramAlerter:
    def send_alert(self, message: str, priority: AlertPriority = AlertPriority.INFO) -> None: ...
```

From src/finalayze/core/trading_loop.py:
```python
class TradingLoop:
    def stop(self) -> None: ...  # shuts down scheduler + async loop + connections
    def start(self) -> None: ...
    # NOTE: no _total_cycles counter exists yet -- must be added
```

From config/settings.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="FINALAYZE_")
    telegram_bot_token: str = ""
    telegram_chat_id: str = ""
    telegram_allowed_chat_ids: list[str] = []
    telegram_webhook_secret: str = ""
    # telegram_admin_chat_id does NOT exist yet -- must be added
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KillSwitch orchestrator with persistent flag and timing</name>
  <files>src/finalayze/core/kill_switch.py, tests/unit/test_kill_switch.py, config/settings.py</files>
  <read_first>
    - src/finalayze/core/alerts.py (AlertPriority enum, send_alert method)
    - src/finalayze/risk/circuit_breaker.py (CircuitLevel enum, override_level method)
    - src/finalayze/execution/broker_router.py (BrokerRouter.registered_markets, route)
    - src/finalayze/execution/tinkoff_broker.py (get_open_orders, cancel_order_safe)
    - src/finalayze/core/trading_loop.py (TradingLoop.stop method)
    - config/settings.py (existing Settings fields)
    - tests/unit/test_telegram_stop_command.py (existing bot command test patterns)
  </read_first>
  <behavior>
    - Test 1: activate() calls cancel_order_safe for each open order across all markets
    - Test 2: activate() calls trading_loop.stop()
    - Test 3: activate() calls circuit_breaker.override_level(CircuitLevel.LIQUIDATE) for each breaker
    - Test 4: activate() calls alerter.send_alert with AlertPriority.CRITICAL
    - Test 5: activate() creates persistent flag file at configured path
    - Test 6: is_killed returns True when flag file exists, False when not
    - Test 7: clear_flag() removes the flag file
    - Test 8: activate() returns KillSwitchResult with orders_cancelled, scheduler_stopped, breakers_escalated, alert_sent, elapsed_seconds
    - Test 9: elapsed_seconds < 30 (mock all deps to be fast)
    - Test 10: activate() logs critical event with structlog
    - Test 11: activate() handles broker cancel failures gracefully (continues with other steps)
  </behavior>
  <action>
    1. Add `telegram_admin_chat_id: str = ""` field to Settings in config/settings.py (with comment `# FINALAYZE_TELEGRAM_ADMIN_CHAT_ID`). Add `kill_switch_flag_path: str = "/tmp/finalayze_killed"` field (with comment `# FINALAYZE_KILL_SWITCH_FLAG_PATH`).

    2. Create `tests/unit/test_kill_switch.py` with 11 tests covering all behaviors above. Use unittest.mock to mock BrokerRouter, TradingLoop, CircuitBreaker, TelegramAlerter. Use `tmp_path` fixture for flag file path. Assert elapsed_seconds < 1.0 in mock test (proves no unnecessary delays).

    3. Create `src/finalayze/core/kill_switch.py`:
       - `from __future__ import annotations`
       - Use `TYPE_CHECKING` guard for imports from execution/ and risk/ layers (same pattern as telegram_bot.py)
       - `KillSwitchResult` frozen dataclass with fields: orders_cancelled (int), scheduler_stopped (bool), breakers_escalated (int), alert_sent (bool), elapsed_seconds (float)
       - `KillSwitch` class:
         - Constructor takes: broker_router, trading_loop, circuit_breakers (dict[str, CircuitBreaker]), alerter (TelegramAlerter), flag_path (Path, default Path("/tmp/finalayze_killed"))
         - `activate(reason: str = "manual") -> KillSwitchResult`:
           - Record start time with `time.monotonic()`
           - Step 1: Cancel all orders -- iterate broker_router.registered_markets, for each call route(market_id), then get_open_orders(), then cancel_order_safe(order_id) for each. Wrap in try/except per order (never abort sequence on single failure).
           - Step 2: Stop TradingLoop -- call trading_loop.stop(), wrap in try/except
           - Step 3: Escalate breakers -- iterate circuit_breakers.values(), call override_level(CircuitLevel.LIQUIDATE)
           - Step 4: Write persistent flag file -- flag_path.write_text(f"killed:{reason}:{timestamp}")
           - Step 5: Send CRITICAL alert -- alerter.send_alert with AlertPriority.CRITICAL, message includes reason and KillSwitchResult summary
           - Return KillSwitchResult with elapsed_seconds = time.monotonic() - start
         - `is_killed` property: returns self._flag_path.exists()
         - `clear_flag()`: removes flag file if exists

    4. Run tests: `uv run pytest tests/unit/test_kill_switch.py -x`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_kill_switch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class KillSwitch" src/finalayze/core/kill_switch.py
    - grep -q "class KillSwitchResult" src/finalayze/core/kill_switch.py
    - grep -q "def activate" src/finalayze/core/kill_switch.py
    - grep -q "def is_killed" src/finalayze/core/kill_switch.py
    - grep -q "def clear_flag" src/finalayze/core/kill_switch.py
    - grep -q "AlertPriority.CRITICAL" src/finalayze/core/kill_switch.py
    - grep -q "override_level.*LIQUIDATE" src/finalayze/core/kill_switch.py
    - grep -q "cancel_order_safe" src/finalayze/core/kill_switch.py
    - grep -q "telegram_admin_chat_id" config/settings.py
    - grep -q "kill_switch_flag_path" config/settings.py
    - uv run pytest tests/unit/test_kill_switch.py -x passes with 11+ tests
  </acceptance_criteria>
  <done>KillSwitch.activate() orchestrates full shutdown sequence (cancel orders, stop loop, escalate breakers, alert) and all 11 tests pass including timing assertion</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HealthMonitor with heartbeat and 2-miss alerting</name>
  <files>src/finalayze/monitoring/health_monitor.py, tests/unit/test_health_monitor.py, src/finalayze/core/trading_loop.py</files>
  <read_first>
    - src/finalayze/core/trading_loop.py (TradingLoop class, scheduler, cycle counters)
    - src/finalayze/core/alerts.py (TelegramAlerter, AlertPriority)
    - src/finalayze/execution/broker_router.py (BrokerRouter)
    - src/finalayze/api/v1/system.py (_check_tinkoff, _check_feed_freshness patterns)
    - src/finalayze/monitoring/sandbox_monitor.py (existing monitoring patterns)
  </read_first>
  <behavior>
    - Test 1: HealthCheckResult is frozen dataclass with broker_ok, feed_fresh, loop_alive, timestamp, details
    - Test 2: check_now() returns HealthCheckResult with correct broker status
    - Test 3: check_now() detects stale feed (last candle > 30 min ago)
    - Test 4: check_now() detects dead loop (cycle count unchanged between checks)
    - Test 5: Single failure increments consecutive_failures to 1, no alert
    - Test 6: Two consecutive failures triggers IMPORTANT alert via alerter
    - Test 7: Successful check after failure resets consecutive_failures to 0
    - Test 8: start() creates APScheduler job, stop() shuts it down
    - Test 9: _total_cycles counter on TradingLoop increments each cycle
  </behavior>
  <action>
    1. Add `_total_cycles: int = 0` counter to TradingLoop.__init__ in `src/finalayze/core/trading_loop.py`. Increment it at the start of `_strategy_cycle` method (find the method and add `self._total_cycles += 1` as first line). Add a `@property` `total_cycles` that returns `self._total_cycles`.

    2. Create `tests/unit/test_health_monitor.py` with 9 tests covering all behaviors. Mock BrokerRouter (get_open_orders returning success/failure), TradingLoop (total_cycles property), TelegramAlerter. Use freezegun or manual datetime for feed freshness tests.

    3. Create `src/finalayze/monitoring/health_monitor.py`:
       - `from __future__ import annotations`
       - Use `TYPE_CHECKING` guard for imports from execution/ and core/ layers
       - `HealthCheckResult` frozen dataclass: broker_ok (bool), feed_fresh (bool), loop_alive (bool), timestamp (datetime), details (dict[str, str])
       - `HealthMonitor` class:
         - Constructor takes: broker_router, trading_loop, alerter, check_interval_seconds (int = 300), feed_freshness_minutes (int = 30)
         - `_consecutive_failures: int = 0`
         - `_last_cycle_count: int = 0`
         - `_last_result: HealthCheckResult | None = None`
         - `_scheduler: BackgroundScheduler | None = None`
         - `check_now() -> HealthCheckResult`:
           - Check broker: try broker_router.route(first registered market).get_open_orders() -- success = broker_ok. Wrap in try/except with 5s timeout concept (catch all exceptions).
           - Check feed: use `_check_feed_freshness()` pattern from system.py -- if last feed update > feed_freshness_minutes, feed_fresh=False. For simplicity, use a `_last_feed_timestamp` attribute that gets updated externally or check via broker.
           - Check loop: compare trading_loop.total_cycles with _last_cycle_count. If same and _last_cycle_count > 0, loop is dead. Update _last_cycle_count.
           - Build details dict with human-readable status per component.
           - Return HealthCheckResult.
         - `_heartbeat() -> None`:
           - Call check_now()
           - If all OK: reset _consecutive_failures = 0
           - If any NOT OK: increment _consecutive_failures
           - If _consecutive_failures >= 2: send alert via alerter.send_alert with AlertPriority.IMPORTANT, include which checks failed
           - Log result via structlog
         - `start() -> None`: Create BackgroundScheduler, add_job(_heartbeat, IntervalTrigger(seconds=check_interval_seconds)), scheduler.start()
         - `stop() -> None`: scheduler.shutdown(wait=False) if running
         - `@property last_result -> HealthCheckResult | None`
         - `update_feed_timestamp(ts: datetime) -> None`: sets `_last_feed_timestamp`

    4. Update `src/finalayze/monitoring/__init__.py` to export HealthMonitor and HealthCheckResult.

    5. Run tests: `uv run pytest tests/unit/test_health_monitor.py -x`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_health_monitor.py tests/unit/test_trading_loop.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class HealthMonitor" src/finalayze/monitoring/health_monitor.py
    - grep -q "class HealthCheckResult" src/finalayze/monitoring/health_monitor.py
    - grep -q "def check_now" src/finalayze/monitoring/health_monitor.py
    - grep -q "_consecutive_failures" src/finalayze/monitoring/health_monitor.py
    - grep -q "AlertPriority.IMPORTANT" src/finalayze/monitoring/health_monitor.py
    - grep -q "BackgroundScheduler" src/finalayze/monitoring/health_monitor.py
    - grep -q "_total_cycles" src/finalayze/core/trading_loop.py
    - grep -q "total_cycles" src/finalayze/core/trading_loop.py
    - uv run pytest tests/unit/test_health_monitor.py -x passes with 9+ tests
  </acceptance_criteria>
  <done>HealthMonitor performs 3 health checks (broker, feed, loop), tracks consecutive failures, alerts on 2 misses with IMPORTANT priority, and all 9 tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_kill_switch.py tests/unit/test_health_monitor.py -x` -- all tests pass
- `uv run ruff check src/finalayze/core/kill_switch.py src/finalayze/monitoring/health_monitor.py` -- no lint errors
- KillSwitch uses CRITICAL priority (OPS-03 compliance)
- HealthMonitor uses IMPORTANT priority (OPS-03 compliance)
</verification>

<success_criteria>
- KillSwitch.activate() runs full shutdown sequence and returns KillSwitchResult with elapsed < 30s (in test)
- HealthMonitor.check_now() returns structured health status
- 2 consecutive health failures trigger Telegram IMPORTANT alert
- Persistent kill flag blocks system (is_killed property)
- All tests pass, no lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-production-operations/17-01-SUMMARY.md`
</output>

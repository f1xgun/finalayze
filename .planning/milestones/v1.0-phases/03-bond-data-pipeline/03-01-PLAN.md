---
phase: 03-bond-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/finalayze/core/bond_math.py
  - src/finalayze/core/bond_math_quantlib.py
  - src/finalayze/core/schemas.py
  - tests/unit/test_bond_math_quantlib.py
  - tests/unit/test_bond_math.py
autonomous: true
requirements: [BDP-02, BDP-04]

must_haves:
  truths:
    - "OFZ-PK floating rate bonds produce economically valid YTM values (within 200bps of market quotes)"
    - "Fixed-coupon bond YTM from QuantLib matches bond_math.py within 1bps"
    - "FloatingRateBond prices OFZ-PK with RUONIA flat curve"
    - "Effective duration computed via +/-25bps rate shock for floaters"
    - "AmortizingFixedRateBond handles decreasing nominal schedule"
    - "NKD supports per-bond day-count convention (not hardcoded actual/365)"
    - "BondInfo schema extended with amortization and inflation-linked fields"
  artifacts:
    - path: "src/finalayze/core/bond_math_quantlib.py"
      provides: "QuantLib wrapper for floaters, amortizers, rate shock duration"
      exports: ["price_fixed_bond_ql", "price_floating_rate_bond", "price_amortizing_bond", "effective_duration_rate_shock", "to_ql_date", "from_ql_date"]
    - path: "tests/unit/test_bond_math_quantlib.py"
      provides: "Validation suite comparing QuantLib vs bond_math.py"
      min_lines: 80
  key_links:
    - from: "src/finalayze/core/bond_math_quantlib.py"
      to: "src/finalayze/core/bond_math.py"
      via: "validation test cross-check"
      pattern: "bond_math\\.ytm.*bond_math_quantlib\\.price_fixed"
---

<objective>
Install QuantLib and build the hybrid bond math engine: QuantLib wrapper for floaters/amortizers alongside existing bond_math.py for fixed-coupon bonds. Extend BondInfo schema with amortization and inflation-linked fields. Add per-bond day-count convention support to NKD calculation.

Purpose: Foundation layer (Layer 0) that all bond pricing depends on. Phase 4 (BondCycleProcessor) and Plan 03 (bond discovery) both consume these math functions.
Output: bond_math_quantlib.py with QuantLib wrappers, extended schemas, validation test suite
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bond-data-pipeline/03-CONTEXT.md
@.planning/phases/03-bond-data-pipeline/03-RESEARCH.md

@src/finalayze/core/bond_math.py
@src/finalayze/core/schemas.py
@tests/unit/test_bond_math.py

<interfaces>
<!-- Key types the executor needs -->

From src/finalayze/core/schemas.py:
```python
class BondInfo(BaseModel):
    figi: str
    ticker: str
    isin: str
    name: str
    face_value: Decimal
    coupon_rate: Decimal  # annual % (e.g. 7.10 for 7.10%)
    coupon_frequency: int  # payments per year (2 for semiannual)
    maturity_date: date
    floating_coupon: bool = False
    class_code: str = "TQOB"
    currency: str = "RUB"
```

From src/finalayze/core/bond_math.py:
```python
def nkd(face_value, coupon_rate, coupon_frequency, last_coupon_date, settlement_date) -> Decimal
def dirty_price(clean_price_pct, face_value, coupon_rate, coupon_frequency, last_coupon_date, settlement_date) -> Decimal
def ytm(face_value, coupon_rate, coupon_frequency, maturity_date, clean_price_pct, settlement_date) -> Decimal
def modified_duration(face_value, coupon_rate, coupon_frequency, maturity_date, clean_price_pct, settlement_date) -> Decimal
def convexity(...) -> Decimal
def dv01(...) -> Decimal
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install QuantLib, extend BondInfo schema, add day-count to NKD</name>
  <files>pyproject.toml, src/finalayze/core/schemas.py, src/finalayze/core/bond_math.py, tests/unit/test_bond_math.py</files>
  <behavior>
    - Test: BondInfo accepts amortization_flag, inflation_linked, initial_nominal, day_count_convention fields
    - Test: CouponPayment accepts is_floating field
    - Test: nkd() with day_count="actual/365" matches existing behavior
    - Test: nkd() with day_count="30/360" produces different (correct) result for same inputs
  </behavior>
  <action>
    1. Run `uv add QuantLib` to install QuantLib and update pyproject.toml/uv.lock
    2. Extend BondInfo in schemas.py with new optional fields per user decision:
       - amortization_flag: bool = False
       - inflation_linked: bool = False
       - initial_nominal: Decimal | None = None (original face value before amortization)
       - day_count_convention: str = "actual/365" (read from T-Invest metadata)
       - bond_type: str = "fixed" (one of: "fixed", "floating", "amortizing", "inflation_linked")
    3. Extend CouponPayment with: is_floating: bool = False
    4. Add day_count parameter to nkd() in bond_math.py. Support "actual/365" (default, existing behavior) and "30/360". Pass through to dirty_price() as well.
    5. Verify QuantLib imports: `import QuantLib as ql` works
    6. Update existing tests to pass with new optional fields (backward compatible)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_bond_math.py -x -q && python -c "import QuantLib; print(f'QuantLib {QuantLib.__version__} OK')"</automated>
  </verify>
  <done>QuantLib installed, BondInfo extended with amortization/inflation/day_count fields, nkd() supports per-bond day count convention, all existing bond_math tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bond_math_quantlib.py with QuantLib wrappers and validation suite</name>
  <files>src/finalayze/core/bond_math_quantlib.py, tests/unit/test_bond_math_quantlib.py</files>
  <behavior>
    - Test: to_ql_date/from_ql_date roundtrip preserves date
    - Test: price_fixed_bond_ql YTM matches bond_math.ytm within 1bps for 5 known OFZ-PD bonds
    - Test: price_floating_rate_bond returns (clean_price, ytm) for OFZ-PK with RUONIA=21%
    - Test: price_amortizing_bond handles decreasing nominal schedule
    - Test: effective_duration_rate_shock returns positive duration for floater
    - Test: effective_duration_rate_shock returns value within 0.5 of analytical duration for fixed bond
  </behavior>
  <action>
    Create src/finalayze/core/bond_math_quantlib.py (Layer 0, no I/O) with:
    1. Date conversion helpers: to_ql_date(d: date) -> ql.Date and from_ql_date(d: ql.Date) -> date
       - QuantLib Date(day, month, year) uses integer month, Python date(year, month, day) -- be careful
    2. price_fixed_bond_ql(settlement_date, maturity_date, face_value, coupon_rate, coupon_frequency, clean_price_pct) -> tuple[Decimal, Decimal]
       - Returns (ytm, modified_duration) using ql.FixedRateBond
       - Use ql.Russia() calendar for schedule, ql.Actual365Fixed() day count
    3. price_floating_rate_bond(settlement_date, maturity_date, face_value, spread, ruonia_rate, coupon_frequency=2) -> tuple[float, float]
       - Returns (clean_price, ytm) per RESEARCH.md pattern
       - Build flat RUONIA forward curve via ql.FlatForward (MVP simplification)
       - Create RUONIA OvernightIndex
       - Use ql.FloatingRateBond with DiscountingBondEngine
    4. price_amortizing_bond(settlement_date, maturity_date, face_value, coupon_rate, coupon_frequency, amortization_schedule: list[tuple[date, Decimal]]) -> tuple[Decimal, Decimal]
       - amortization_schedule = [(date, remaining_nominal_pct), ...]
       - Use ql.AmortizingFixedRateBond or build custom cashflow schedule
       - Returns (ytm, modified_duration)
    5. effective_duration_rate_shock(bond, yield_curve_handle, shock_bps=25) -> float
       - Per RESEARCH.md pattern: reprice with +/-25bps parallel shift, numerical duration
       - Works for any bond type (fixed, floating, amortizing)
    6. build_ruonia_curve(settlement_date, ruonia_rate) -> ql.YieldTermStructureHandle
       - Shared helper for curve construction

    Create tests/unit/test_bond_math_quantlib.py with:
    - Validation test: Compare QuantLib vs bond_math.py for 5 known OFZ-PD bonds (SU26238RMFS4, SU26243RMFS4, etc.)
    - Floater test: OFZ-PK with known parameters, verify clean price in reasonable range
    - Amortizer test: Simple amortizing bond with 2 amortization events
    - Duration test: Effective duration via shock vs analytical for fixed bond
    - Date conversion roundtrip test

    IMPORTANT: Use ql.Russia() calendar for QuantLib scheduling but our moex_calendar.py for business day checks outside QuantLib. Per RESEARCH.md, QuantLib Russia() may not have all transferred holidays.

    IMPORTANT: MOEX quotes bond prices as percentage of face value (e.g. 85.50%). All price parameters should be clean_price_pct (Decimal, % of face). Document convention on every function.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_bond_math_quantlib.py -x -v</automated>
  </verify>
  <done>QuantLib wrapper provides YTM/duration for fixed, floating, and amortizing bonds. Validation suite confirms QuantLib matches bond_math.py for fixed-coupon bonds within 1bps. Effective duration via rate shock works for all bond types.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_bond_math.py tests/unit/test_bond_math_quantlib.py -x -v` -- all pass
- `uv run ruff check src/finalayze/core/bond_math_quantlib.py src/finalayze/core/bond_math.py` -- no lint errors
- `python -c "import QuantLib; print(QuantLib.__version__)"` -- prints version
</verification>

<success_criteria>
- QuantLib installed and importable
- bond_math_quantlib.py exports all 6 functions listed in must_haves
- Validation test confirms QuantLib YTM matches bond_math.py within 1bps for fixed-coupon bonds
- FloatingRateBond prices OFZ-PK with flat RUONIA curve
- AmortizingFixedRateBond handles decreasing nominal
- Effective duration via +/-25bps shock works for fixed and floating bonds
- BondInfo schema extended with amortization/inflation/day_count fields (backward compatible)
- NKD supports per-bond day-count convention
</success_criteria>

<output>
After completion, create `.planning/phases/03-bond-data-pipeline/03-01-SUMMARY.md`
</output>

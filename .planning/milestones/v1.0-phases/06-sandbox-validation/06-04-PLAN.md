---
phase: 06-sandbox-validation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - src/finalayze/core/trading_loop.py
  - tests/unit/test_settings_extra_ignore.py
autonomous: true
gap_closure: true
requirements:
  - AUT-04
  - AUT-06

must_haves:
  truths:
    - "Settings ignores non-FINALAYZE_ env vars from .env without validation errors"
    - "CycleLogEntry instruments_processed reflects actual instrument count per equity cycle"
    - "CycleLogEntry orders_submitted and orders_filled reflect actual order activity per cycle"
    - "All existing API tests pass without pydantic collection errors"
  artifacts:
    - path: "config/settings.py"
      provides: "Settings model_config with extra=ignore"
      contains: "extra"
    - path: "src/finalayze/core/trading_loop.py"
      provides: "Wired cycle counters for instruments, signals, orders"
  key_links:
    - from: "src/finalayze/core/trading_loop.py"
      to: "src/finalayze/core/validation_logger.py"
      via: "CycleLogEntry with real counters"
      pattern: "instruments_processed=.*[^0]"
---

<objective>
Fix two verification gaps from Phase 6: (1) Settings model rejects Docker Compose env vars from .env, breaking API tests; (2) CycleLogEntry trade counters are hardcoded to zero, making validation report trade-count criterion unreliable.

Purpose: Unblock API test suite and ensure validation report accurately reflects trading activity.
Output: Patched settings.py and trading_loop.py with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sandbox-validation/06-VERIFICATION.md
@config/settings.py
@src/finalayze/core/trading_loop.py
@src/finalayze/core/validation_logger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extra="ignore" to Settings model_config</name>
  <files>config/settings.py, tests/unit/test_settings_extra_ignore.py</files>
  <action>
In config/settings.py, change line 111:
```python
model_config = {"env_prefix": "FINALAYZE_", "env_file": ".env"}
```
to:
```python
model_config = {"env_prefix": "FINALAYZE_", "env_file": ".env", "extra": "ignore"}
```

This makes pydantic-settings silently ignore non-prefixed env vars like POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, REDIS_PASSWORD that Docker Compose adds to .env.

Create tests/unit/test_settings_extra_ignore.py with tests:
- Test that Settings() does not raise when env contains extra non-FINALAYZE_ keys (monkeypatch env with POSTGRES_USER=x, POSTGRES_DB=y)
- Test that FINALAYZE_-prefixed vars are still loaded correctly
- Test that unknown FINALAYZE_-prefixed vars are also ignored (not just non-prefixed ones)

Do NOT modify .env itself or move Docker vars to a separate file.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_extra_ignore.py tests/unit/test_api_health.py tests/unit/test_api_system.py -x -q</automated>
  </verify>
  <done>Settings model accepts .env files with Docker Compose variables. All API test files collect and run without pydantic ValidationError.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CycleLogEntry trade counters from _strategy_cycle_impl</name>
  <files>src/finalayze/core/trading_loop.py</files>
  <action>
Wire actual counters into CycleLogEntry for the equity cycle. The approach:

1. Add instance-level cycle counters to TradingLoop (reset at start of each cycle):
   - `self._cycle_instruments_processed: int = 0`
   - `self._cycle_signals_generated: int = 0`
   - `self._cycle_orders_submitted: int = 0`
   - `self._cycle_orders_filled: int = 0`

2. In `_strategy_cycle()` (around line 782), reset all counters to 0 before calling `_strategy_cycle_impl()`.

3. In `_process_market_cycle()` (around line 894), after `instruments = self._registry.list_by_market(market_id)`, add:
   `self._cycle_instruments_processed += len(instruments)`

4. In `_process_instrument()`:
   - After `signal = self._strategy.generate_signal(...)` returns a non-None signal (around line 1016), increment `self._cycle_signals_generated += 1`
   - After `self._submit_order(order, ...)` (around line 1126), increment `self._cycle_orders_submitted += 1`

5. In `_submit_order()`, after a successful broker.submit_order() call, increment `self._cycle_orders_filled += 1`. If submit_order raises, do not increment (the order was not filled).

6. In `_strategy_cycle()` finally block (around line 802-814), replace the hardcoded zeros:
   ```python
   instruments_processed=self._cycle_instruments_processed,
   signals_generated=self._cycle_signals_generated,
   orders_submitted=self._cycle_orders_submitted,
   orders_filled=self._cycle_orders_filled,
   ```

7. For the bond cycle (lines 562-565), the counters remain 0 for now since bond cycle metrics come from BondCycleProcessor.run_cycle() result which has its own tracking. This is acceptable -- bond cycle log entries already capture errors_caught correctly.

Initialize the counter attributes in __init__ to 0.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_validation_logger.py tests/unit/test_trading_loop_jobstore.py -x -q && uv run ruff check config/settings.py src/finalayze/core/trading_loop.py && uv run ruff format --check config/settings.py src/finalayze/core/trading_loop.py</automated>
  </verify>
  <done>CycleLogEntry for equity cycles contains actual instruments_processed, signals_generated, orders_submitted counts. No more TODO comments for cycle counters in _strategy_cycle(). Lint and format checks pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/unit/test_api_health.py -x` -- previously broken by pydantic extra fields, now passes
2. `uv run pytest tests/unit/ -x -q` -- full unit test suite passes
3. `uv run ruff check config/ src/finalayze/core/trading_loop.py` -- no lint errors
4. Grep for `instruments_processed=0` in trading_loop.py equity cycle -- should NOT appear (bond cycle 0s are acceptable)
</verification>

<success_criteria>
- All API test files (test_api_health, test_api_system, test_cors, test_api_portfolio, etc.) collect and pass without pydantic ValidationError
- CycleLogEntry equity cycle counters use instance variables instead of hardcoded zeros
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-sandbox-validation/06-04-SUMMARY.md`
</output>

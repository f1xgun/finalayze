---
phase: 02-moex-equity-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/test_pairs_cointegration.py
  - src/finalayze/strategies/presets/ru_blue_chips.yaml
  - src/finalayze/strategies/presets/ru_energy.yaml
  - src/finalayze/strategies/presets/ru_finance.yaml
autonomous: false
requirements: [EQF-02, EQF-03]

must_haves:
  truths:
    - "Walk-forward backtest shows positive PnL on at least 2 MOEX segments"
    - "Out-of-sample Sharpe > 0.1 on at least 2 MOEX segments"
    - "Out-of-sample Profit Factor > 1.05 on at least 2 MOEX segments"
    - "Max drawdown < 20% on all segments"
    - "Only cointegrated pairs (p < 0.05) remain in presets"
    - "ru_* YAML presets contain MOEX-calibrated parameters (not US defaults)"
  artifacts:
    - path: "src/finalayze/strategies/presets/ru_blue_chips.yaml"
      provides: "Optuna-calibrated MOEX-specific parameters for balanced segment"
    - path: "src/finalayze/strategies/presets/ru_energy.yaml"
      provides: "Optuna-calibrated MOEX-specific parameters for energy (momentum-tilted) segment"
    - path: "src/finalayze/strategies/presets/ru_finance.yaml"
      provides: "Optuna-calibrated MOEX-specific parameters for finance (MR-tilted) segment"
    - path: "results/iterations/moex-phase2-calibrated/summary.json"
      provides: "Walk-forward OOS Sharpe, PF, and DD for all 3 MOEX segments"
  key_links:
    - from: "src/finalayze/strategies/presets/ru_*.yaml"
      to: "scripts/run_iteration.py"
      via: "YAML preset loading by StrategyCombiner"
      pattern: "segment_id.*ru_"
    - from: "scripts/tune_strategy_params.py"
      to: "src/finalayze/strategies/presets/ru_*.yaml"
      via: "Optuna trial applying candidate params"
      pattern: "yaml.*dump"
---

<objective>
Run cointegration tests on MOEX pairs, Optuna-tune strategy parameters per segment, and validate with walk-forward backtests to achieve positive PnL on 2+ MOEX segments.

Purpose: This is the core calibration work. Current MOEX presets are US copies producing negative Sharpe (-0.4 to -0.6). Genuine MOEX-specific parameter tuning with Optuna guardrails should achieve the target: OOS Sharpe > 0.1 and PF > 1.05 on at least 2 segments.

Output: Calibrated ru_* YAML presets with walk-forward validated positive performance, cointegration-tested pairs.

Note: This plan satisfies the CLAUDE.md mandatory backtest-iteration gate for both Plan 01 (YAML preset changes) and Plan 02 (Optuna tuning). Task 2 runs full walk-forward backtests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-moex-equity-validation/02-CONTEXT.md
@.planning/phases/02-moex-equity-validation/02-RESEARCH.md
@.planning/phases/02-moex-equity-validation/02-01-SUMMARY.md

@scripts/tune_strategy_params.py
@scripts/test_pairs_cointegration.py
@scripts/run_iteration.py
@scripts/run_strategy_isolation.py
@src/finalayze/strategies/presets/ru_blue_chips.yaml
@src/finalayze/strategies/presets/ru_energy.yaml
@src/finalayze/strategies/presets/ru_finance.yaml
@.claude/skills/backtest-iteration.md
@.claude/skills/preset-tuner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cointegration testing and Optuna tuning</name>
  <files>
    scripts/test_pairs_cointegration.py
    src/finalayze/strategies/presets/ru_blue_chips.yaml
    src/finalayze/strategies/presets/ru_energy.yaml
    src/finalayze/strategies/presets/ru_finance.yaml
  </files>
  <action>
    **Step 1: Run cointegration tests on all pairs**

    Run the fixed cointegration script (from Plan 01) to test all 8 candidate pairs:
    ```bash
    uv run python scripts/test_pairs_cointegration.py
    ```

    From results, apply decision criteria:
    - KEEP pair if: p-value < 0.05 AND half-life < 30 days AND Hurst < 0.5
    - DISABLE pair if any criterion fails
    - Update pairs lists in all 3 ru_* YAML presets: only include passing pairs
    - If ALL pairs fail cointegration, disable pairs strategy entirely on affected segments and redistribute weight
    - For borderline cases (p-value 0.05-0.10): use Claude's discretion per CONTEXT.md, but lean toward dropping

    **Step 2: Optuna tuning per segment**

    Run Optuna tuning for each MOEX segment. Maximum 5 tuning iterations total (per user decision). Use existing guardrails (DSR haircut, holdout validation, perturbation check).

    ```bash
    uv run python scripts/tune_strategy_params.py --segment ru_blue_chips --n-trials 50
    uv run python scripts/tune_strategy_params.py --segment ru_energy --n-trials 50
    uv run python scripts/tune_strategy_params.py --segment ru_finance --n-trials 50
    ```

    After each Optuna run:
    1. Review best trial params -- check they pass perturbation test and holdout validation
    2. Apply best params to the YAML preset ONLY IF they improve OOS metrics
    3. Record metrics for comparison

    Key parameters to include in Optuna search space (discretion areas from CONTEXT.md):
    - ADX regime routing thresholds (trend_threshold, mr_threshold)
    - Bollinger Band std_dev (no floor constraint)
    - max_hold_bars
    - min_combined_confidence, min_exit_confidence
    - Strategy-specific min_confidence values
    - OU window size
    - Dual momentum lookback periods

    **Anti-overfitting discipline:**
    - Walk-forward OOS metrics are the gate, NOT in-sample
    - Reject any parameter set with max drawdown > 20%
    - If holdout degradation ratio < 0.50, reject that trial
    - Track trade count: PF with < 30 trades is noise
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_moex_preset_validation.py tests/unit/test_strategy_combiner.py tests/unit/test_backtest_config.py -x -q</automated>
  </verify>
  <done>
    - Cointegration results recorded for all 8 candidate pairs with keep/disable decisions
    - Only cointegrated pairs remain in YAML presets
    - Optuna tuning completed for all 3 segments with guardrails applied
    - Best-trial params applied to YAML presets (or baseline kept if Optuna found no improvement)
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk-forward validation and final iteration comparison</name>
  <files>
    src/finalayze/strategies/presets/ru_blue_chips.yaml
    src/finalayze/strategies/presets/ru_energy.yaml
    src/finalayze/strategies/presets/ru_finance.yaml
  </files>
  <!-- Note: scripts/run_iteration.py is invoked read-only (not modified) -->
  <action>
    **Step 1: Run full walk-forward backtest on all 3 MOEX segments**

    Use the backtest-iteration skill to run and compare:
    ```bash
    uv run python scripts/run_iteration.py \
      --name "moex-phase2-calibrated" \
      --description "MOEX presets after isolation + cointegration + Optuna tuning" \
      --segments ru_blue_chips,ru_energy,ru_finance
    ```

    **Step 2: Evaluate against performance targets**

    From walk-forward results, check each segment against targets:
    - OOS Sharpe > 0.1 (minimum bar)
    - Profit Factor > 1.05 (minimum bar)
    - Max Drawdown < 20% (hard reject)
    - At least 2 of 3 segments must pass ALL criteria

    **Step 3: If targets NOT met (iterate, max 5 total iterations)**

    If fewer than 2 segments pass:
    1. Diagnose using strategy-diagnose skill: identify which strategies drag performance
    2. Adjust strategy weights based on isolation results from Plan 01
    3. Consider reducing ADX thresholds if momentum strategies fire too rarely
    4. Re-run walk-forward and check again
    5. Maximum 5 total tuning iterations (counting Optuna runs from Task 1)

    **Step 4: If targets still NOT met after 5 iterations**

    Document the best achieved metrics and note which segments passed/failed. Accept the result per user decision ("maximum 5 tuning iterations before accepting results or pivoting approach"). The presets should reflect the best-achieved configuration even if targets are not fully met.

    **Step 5: Record final results**

    Create a comparison table of baseline vs final metrics per segment. Run the backtest-iteration skill for the final comparison:
    - Compare against `moex-new-datasources` baseline (Sharpe -0.43/-0.66)
    - Record improvement deltas
  </action>
  <verify>
    <automated>ls results/iterations/moex-phase2-calibrated/ && uv run python -c "
import json, pathlib, sys
# Check that walk-forward results directory exists and has output
results_dir = pathlib.Path('results/iterations/moex-phase2-calibrated')
files = list(results_dir.iterdir())
if not files:
    print('FAIL: results directory is empty')
    sys.exit(1)
print(f'OK: moex-phase2-calibrated has {len(files)} result files')
# Check summary if it exists
summary = results_dir / 'summary.json'
if summary.exists():
    data = json.loads(summary.read_text())
    print(f'Summary keys: {list(data.keys())}')
else:
    print('NOTE: summary.json not found -- check output format')
"</automated>
  </verify>
  <done>
    - Walk-forward backtest completed on all 3 MOEX segments
    - Results directory `results/iterations/moex-phase2-calibrated/` exists with output files
    - At least 2 segments show OOS Sharpe > 0.1 AND PF > 1.05 AND DD < 20% (or best-effort after 5 iterations documented)
    - Final iteration comparison recorded against baseline
    - ru_* YAML presets reflect the calibrated MOEX-specific parameters
    - This satisfies CLAUDE.md mandatory backtest-iteration gate for Plan 01 and Plan 02 YAML changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MOEX calibration results</name>
  <action>Human verification of walk-forward backtest results against performance targets</action>
  <what-built>MOEX equity strategy calibration: all strategies enabled, cointegration-tested pairs, Optuna-tuned parameters, walk-forward validated performance</what-built>
  <how-to-verify>
    1. Review the walk-forward metrics table in the execution output
    2. Confirm at least 2 MOEX segments show positive OOS Sharpe (> 0.1) and PF (> 1.05)
    3. Check that max drawdown is below 20% on all segments
    4. Review the ru_* YAML presets to confirm MOEX-specific parameters (not US copies)
    5. If targets were not fully met after 5 iterations, review documented best-effort results
    6. Optional: run `uv run python scripts/run_iteration.py --segments ru_blue_chips,ru_energy,ru_finance` yourself to verify metrics
  </how-to-verify>
  <resume-signal>Type "approved" if results meet expectations, or describe concerns/adjustments needed</resume-signal>
</task>

</tasks>

<verification>
- Walk-forward OOS Sharpe > 0.1 on at least 2 MOEX segments
- Walk-forward OOS PF > 1.05 on at least 2 MOEX segments
- Max drawdown < 20% on all segments
- All YAML presets have strategy weights summing to ~1.0
- No pairs with failed cointegration (p > 0.05) remain in presets
- Existing unit tests pass: `uv run pytest tests/unit/ -x -q`
- Results directory exists: `ls results/iterations/moex-phase2-calibrated/`
</verification>

<success_criteria>
- EQF-02: MOEX backtest produces positive PnL with walk-forward validation on 2+ segments
- EQF-03: ru_* YAML presets calibrated with genuine MOEX-specific parameters (ADX thresholds, BB std_dev, OU window, momentum lookbacks -- all tuned, not US defaults)
- Improvement over baseline: OOS Sharpe improved from -0.43/-0.66 to > 0.1 on 2+ segments
</success_criteria>

<output>
After completion, create `.planning/phases/02-moex-equity-validation/02-02-SUMMARY.md`
</output>

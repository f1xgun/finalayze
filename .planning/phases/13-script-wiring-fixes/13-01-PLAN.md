---
phase: 13-script-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_iteration.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
gap_closure: true

must_haves:
  truths:
    - "run_iteration.py UNIVERSE dict for all ru_* segments excludes GAZP, VTBR, SNGS, SNGSP, IRAO, ALRS"
    - "DividendEntry.status= is passed in all 3 data loading paths of _setup_dividend_gap_strategy"
  artifacts:
    - path: "scripts/run_iteration.py"
      provides: "Synced UNIVERSE dict and DividendEntry.status wiring"
      contains: "status="
  key_links:
    - from: "scripts/run_iteration.py UNIVERSE dict"
      to: "config/segments.py DEFAULT_SEGMENTS"
      via: "same symbol sets"
      pattern: "ru_blue_chips.*SBER|ru_energy.*LKOH"
    - from: "_setup_dividend_gap_strategy DividendEntry"
      to: "DividendEntry.status field"
      via: "keyword argument"
      pattern: "DividendEntry.*status="
---

<objective>
Close two audit integration gaps in scripts/run_iteration.py:
1. Sync the UNIVERSE dict with config/segments.py by removing toxic symbols from all ru_* entries.
2. Wire DividendEntry.status= in all 3 data loading paths of _setup_dividend_gap_strategy.

Purpose: The core logic (config/segments.py, DividendEntry.status field) is already correct. These are pure script-level wiring gaps that prevent the implemented fixes from taking effect during iteration runs.
Output: scripts/run_iteration.py with toxic symbols removed and status passed in all 3 DividendEntry constructor calls.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove toxic symbols from UNIVERSE dict and wire DividendEntry.status</name>
  <files>scripts/run_iteration.py, tests/unit/test_run_iteration_universe.py</files>
  <behavior>
    - Test 1: ru_blue_chips UNIVERSE entry must not contain any of: GAZP, VTBR, ALRS, SNGS, SNGSP, IRAO
    - Test 2: ru_energy UNIVERSE entry must not contain GAZP or SNGS
    - Test 3: ru_finance UNIVERSE entry must not contain VTBR
    - Test 4: _setup_dividend_gap_strategy path 1 (Tinkoff API) creates DividendEntry with status="paid"
    - Test 5: _setup_dividend_gap_strategy path 2 (event data JSON) creates DividendEntry with status from entry or "paid" default
    - Test 6: _setup_dividend_gap_strategy path 3 (static YAML) creates DividendEntry with status from entry or "paid" default
  </behavior>
  <action>
    Write failing tests first in tests/unit/test_run_iteration_universe.py, then make the following targeted edits to scripts/run_iteration.py:

    **Fix 1 — UNIVERSE dict (lines 192-222):**

    ru_blue_chips: remove GAZP (line 195), ALRS (line 198), VTBR (line 199).
    Keep: SBER, LKOH, YNDX, MGNT, POLY, NVTK, MTLR.

    ru_energy: remove GAZP (line 206), SNGS (line 211).
    Keep: LKOH, ROSN, NVTK, TATN, TRNFP, BANEP.

    ru_finance: remove VTBR (line 218).
    Keep: SBER, SBERP, TCSG, CBOM, BSPB, MOEX.

    Note: SNGSP does not appear in the current dict — no change needed. IRAO does not appear — no change needed.

    **Fix 2 — DividendEntry.status wiring in _setup_dividend_gap_strategy (~line 363):**

    Path 1 (Tinkoff API, ~line 392): Change:
      DividendEntry(ex_date=div["ex_date"], amount=div["amount"])
    to:
      DividendEntry(ex_date=div["ex_date"], amount=div["amount"], status="paid")

    Path 2 (event data JSON, ~line 408): Change:
      DividendEntry(
          ex_date=datetime.strptime(entry["ex_date"], "%Y-%m-%d").replace(tzinfo=UTC),
          amount=float(entry["amount"]),
      )
    to:
      DividendEntry(
          ex_date=datetime.strptime(entry["ex_date"], "%Y-%m-%d").replace(tzinfo=UTC),
          amount=float(entry["amount"]),
          status=entry.get("status", "paid"),
      )

    Path 3 (static YAML, ~line 436): Change:
      DividendEntry(ex_date=ex_date, amount=float(entry["amount"]))
    to:
      DividendEntry(ex_date=ex_date, amount=float(entry["amount"]), status=entry.get("status", "paid"))

    No other changes. Do not touch any other functions or logic.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_run_iteration_universe.py -x -q && uv run ruff check scripts/run_iteration.py && uv run ruff format --check scripts/run_iteration.py</automated>
  </verify>
  <done>
    - tests/unit/test_run_iteration_universe.py passes (6 tests green)
    - GAZP, VTBR, ALRS, SNGS absent from UNIVERSE dict ru_* entries
    - All 3 DividendEntry constructor calls include status= keyword argument
    - ruff check and ruff format --check pass on scripts/run_iteration.py
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_run_iteration_universe.py -v
uv run ruff check scripts/run_iteration.py
uv run ruff format --check scripts/run_iteration.py
uv run mypy scripts/run_iteration.py --ignore-missing-imports
</verification>

<success_criteria>
1. UNIVERSE dict ru_blue_chips, ru_energy, ru_finance entries contain no toxic symbols (GAZP, VTBR, SNGS, SNGSP, IRAO, ALRS)
2. All 3 DividendEntry constructor calls in _setup_dividend_gap_strategy pass status= keyword argument
3. All 6 unit tests pass, ruff and mypy clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-script-wiring-fixes/13-01-SUMMARY.md`
</output>

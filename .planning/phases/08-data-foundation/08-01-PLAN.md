---
phase: 08-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/strategies/presets/ru_blue_chips.yaml
  - src/finalayze/strategies/presets/ru_energy.yaml
  - src/finalayze/strategies/presets/ru_finance.yaml
  - src/finalayze/strategies/presets/ru_tech.yaml
  - config/segments.py
  - tests/unit/test_config.py
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "All ru_* segment YAML presets have vol_target: 0.40 (not 0.19-0.22)"
    - "min_combined_confidence is 0.38 on all ru_* segment YAML presets"
    - "GAZP is not in ru_blue_chips symbols list in segments.py"
    - "VTBR is not in ru_finance symbols list in segments.py"
    - "SNGS and SNGSP are not in ru_energy symbols list in segments.py"
    - "IRAO is not in ru_utilities symbols list in segments.py"
    - "ALRS is not in ru_metals symbols list in segments.py"
    - "event_driven strategy is disabled (enabled: false) in all ru_* presets"
  artifacts:
    - path: "src/finalayze/strategies/presets/ru_blue_chips.yaml"
      provides: "MOEX-calibrated vol target and confidence threshold"
      contains: "vol_target: 0.40"
    - path: "src/finalayze/strategies/presets/ru_energy.yaml"
      provides: "MOEX-calibrated vol target and confidence threshold"
      contains: "vol_target: 0.40"
    - path: "src/finalayze/strategies/presets/ru_finance.yaml"
      provides: "MOEX-calibrated vol target and confidence threshold"
      contains: "vol_target: 0.40"
    - path: "src/finalayze/strategies/presets/ru_tech.yaml"
      provides: "MOEX-calibrated vol target and confidence threshold"
      contains: "vol_target: 0.40"
    - path: "config/segments.py"
      provides: "Cleaned MOEX universe without toxic symbols"
  key_links:
    - from: "src/finalayze/strategies/presets/ru_*.yaml"
      to: "src/finalayze/risk/position_sizing_pipeline.py"
      via: "VolTargetStep reads target_vol from preset config"
      pattern: "vol_target: 0\\.40"
---

<objective>
Recalibrate vol targets and clean the MOEX universe so all ru_* backtests produce valid, properly-sized results.

Purpose: Current US-calibrated vol_target (0.19-0.22) crushes MOEX positions to 25-42% of intended size because MOEX blue chip vol is 35-45% annualized. Toxic symbols (GAZP, VTBR, SNGS, IRAO, ALRS) account for ~60% of negative PnL. These config fixes are prerequisites for any valid MOEX backtest.

Output: Updated YAML presets and segments.py with MOEX-appropriate parameters.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-foundation/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ru_* YAML presets -- vol_target, min_combined_confidence, disable event_driven</name>
  <files>
    src/finalayze/strategies/presets/ru_blue_chips.yaml
    src/finalayze/strategies/presets/ru_energy.yaml
    src/finalayze/strategies/presets/ru_finance.yaml
    src/finalayze/strategies/presets/ru_tech.yaml
  </files>
  <read_first>
    src/finalayze/strategies/presets/ru_blue_chips.yaml
    src/finalayze/strategies/presets/ru_energy.yaml
    src/finalayze/strategies/presets/ru_finance.yaml
    src/finalayze/strategies/presets/ru_tech.yaml
    src/finalayze/risk/position_sizing_pipeline.py
  </read_first>
  <action>
In each of the four ru_* YAML preset files, make these exact changes:

1. Change top-level `min_combined_confidence` from its current value to `0.38`
2. For every strategy that has a `vol_target` parameter (momentum, dual_momentum, and any others with `vol_target_enabled: true`), change `vol_target` from its current value (0.19, 0.20, 0.21, or 0.22) to `0.40`
3. Set `event_driven.enabled` to `false` (no real-time news feed; phantom signals in backtest). Keep the weight and params as-is for future re-enablement.

Specific values per file:
- ru_blue_chips.yaml: min_combined_confidence 0.15 -> 0.38, momentum vol_target 0.19 -> 0.40, dual_momentum vol_target 0.19 -> 0.40, event_driven enabled true -> false
- ru_energy.yaml: min_combined_confidence 0.15 -> 0.38, momentum vol_target 0.22 -> 0.40, dual_momentum vol_target 0.22 -> 0.40, event_driven enabled true -> false
- ru_finance.yaml: min_combined_confidence 0.15 -> 0.38, momentum vol_target 0.21 -> 0.40, dual_momentum vol_target 0.21 -> 0.40, event_driven enabled true -> false
- ru_tech.yaml: min_combined_confidence 0.30 -> 0.38, momentum vol_target 0.20 -> 0.40, dual_momentum vol_target 0.20 -> 0.40, event_driven enabled true -> false

Do NOT change any US preset files (us_tech.yaml, us_broad.yaml, etc.). Do NOT change any other parameters.
  </action>
  <verify>
    <automated>grep -c "vol_target: 0.40" src/finalayze/strategies/presets/ru_blue_chips.yaml src/finalayze/strategies/presets/ru_energy.yaml src/finalayze/strategies/presets/ru_finance.yaml src/finalayze/strategies/presets/ru_tech.yaml && grep "min_combined_confidence: 0.38" src/finalayze/strategies/presets/ru_blue_chips.yaml src/finalayze/strategies/presets/ru_energy.yaml src/finalayze/strategies/presets/ru_finance.yaml src/finalayze/strategies/presets/ru_tech.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Each ru_* YAML file contains `min_combined_confidence: 0.38`
    - Each ru_* YAML file contains `vol_target: 0.40` for every strategy that had vol_target_enabled
    - No ru_* YAML file contains `vol_target: 0.19` or `vol_target: 0.20` or `vol_target: 0.21` or `vol_target: 0.22`
    - Each ru_* YAML file has `event_driven: enabled: false`
    - US preset files are unchanged
  </acceptance_criteria>
  <done>All four ru_* YAML presets have vol_target 0.40, min_combined_confidence 0.38, event_driven disabled</done>
</task>

<task type="auto">
  <name>Task 2: Remove toxic symbols from MOEX segments in config/segments.py</name>
  <files>
    config/segments.py
    tests/unit/test_config.py
  </files>
  <read_first>
    config/segments.py
    tests/unit/test_config.py
  </read_first>
  <action>
In config/segments.py, modify the DEFAULT_SEGMENTS list:

1. ru_blue_chips: Remove "GAZP" from symbols list. Result: `symbols=["SBER", "LKOH", "GMKN"]`
2. ru_energy: Remove "SNGS" and "SNGSP" from symbols list. Result: `symbols=["ROSN", "TATN", "NVTK", "SIBN", "TATNP", "TRNFP"]`
3. ru_finance: Remove "VTBR" from symbols list. Result: `symbols=["SBER", "SBERP", "TCSG", "CBOM", "BSPB", "MOEX"]`
4. ru_utilities: Remove "IRAO" from symbols list. Result: `symbols=["HYDR", "FEES", "MSNG", "UPRO"]`
5. ru_metals: Remove "ALRS" from symbols list. Result: `symbols=["GMKN", "CHMF", "NLMK", "MAGN", "PLZL", "RUAL", "POLY", "MTLR"]`

Do NOT change any US segments. Do NOT change any other fields (market, broker, currency, active_strategies, etc.).

In tests/unit/test_config.py, update any tests that reference the removed symbols (GAZP, VTBR, SNGS, SNGSP, IRAO, ALRS) in segment symbol lists. If tests assert specific symbol counts for ru_* segments, update the expected counts. Add a new test `test_toxic_symbols_excluded_from_moex_segments` that asserts:
- "GAZP" not in ru_blue_chips symbols
- "VTBR" not in ru_finance symbols
- "SNGS" not in ru_energy symbols
- "SNGSP" not in ru_energy symbols
- "IRAO" not in ru_utilities symbols
- "ALRS" not in ru_metals symbols
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - config/segments.py does not contain "GAZP" in ru_blue_chips symbols
    - config/segments.py does not contain "VTBR" in ru_finance symbols
    - config/segments.py does not contain "SNGS" or "SNGSP" in ru_energy symbols
    - config/segments.py does not contain "IRAO" in ru_utilities symbols
    - config/segments.py does not contain "ALRS" in ru_metals symbols
    - tests/unit/test_config.py contains `def test_toxic_symbols_excluded_from_moex_segments`
    - `uv run pytest tests/unit/test_config.py -x` exits 0
  </acceptance_criteria>
  <done>Five toxic symbols removed from MOEX segments, test validates exclusion, all config tests pass</done>
</task>

</tasks>

<verification>
1. `grep -c "vol_target: 0.40" src/finalayze/strategies/presets/ru_*.yaml` -- each file shows at least 1 match
2. `grep "GAZP\|VTBR\|SNGS\|SNGSP\|IRAO\|ALRS" config/segments.py` -- no matches in any ru_* segment
3. `uv run pytest tests/unit/test_config.py -x` -- passes
4. `uv run ruff check config/segments.py tests/unit/test_config.py` -- no lint errors
</verification>

<success_criteria>
- All ru_* YAML presets have vol_target 0.40 and min_combined_confidence 0.38
- GAZP, VTBR, SNGS, SNGSP, IRAO, ALRS removed from active MOEX segments
- event_driven disabled in all ru_* presets
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-foundation/08-01-SUMMARY.md`
</output>

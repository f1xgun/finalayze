---
phase: 08-data-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_moex_dividends.py
  - src/finalayze/strategies/presets/moex_dividends.yaml
  - tests/unit/test_dividend_calendar.py
autonomous: true
requirements: [DATA-03]
user_setup:
  - service: tinkoff_invest
    why: "Fetching dividend data from T-Invest API"
    env_vars:
      - name: FINALAYZE_TINKOFF_TOKEN
        source: "T-Invest developer portal -> API token"

must_haves:
  truths:
    - "Dividend calendar contains 150+ events across 20+ symbols"
    - "Calendar includes cancelled/reduced dividends with status field (paid/cancelled/reduced)"
    - "GAZP 2022 cancelled dividend (52.53 RUB recommended, rejected) is present with status: cancelled"
    - "Every entry has ex_date, amount, and status fields"
  artifacts:
    - path: "src/finalayze/strategies/presets/moex_dividends.yaml"
      provides: "Expanded MOEX dividend calendar with 150+ events"
      contains: "status: cancelled"
    - path: "scripts/fetch_moex_dividends.py"
      provides: "Script to batch-fetch dividends from T-Invest API for all ru_* symbols"
    - path: "tests/unit/test_dividend_calendar.py"
      provides: "Tests validating calendar structure and content"
  key_links:
    - from: "src/finalayze/strategies/presets/moex_dividends.yaml"
      to: "src/finalayze/strategies/dividend_gap.py"
      via: "DividendGapStrategy._calendar populated from YAML"
      pattern: "_calendar"
    - from: "scripts/fetch_moex_dividends.py"
      to: "src/finalayze/data/fetchers/tinkoff_data.py"
      via: "Uses TinkoffFetcher._fetch_dividends_async for API calls"
      pattern: "_fetch_dividends_async"
---

<objective>
Expand the MOEX dividend calendar from 43 events across 6 symbols to 150+ events across 20+ symbols, including cancelled/reduced dividends with a status field.

Purpose: The current calendar contains only paid dividends. Cancelled events (like GAZP 2022: 52.53 RUB recommended then rejected) are missing, introducing look-ahead bias. The strategy appears to have 85%+ win rate when real rate is 65-75%. Adding a status field and including all board recommendations eliminates this bias.

Output: Expanded moex_dividends.yaml with 150+ events, a reusable fetch script, and validation tests.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-foundation/08-CONTEXT.md

<interfaces>
From src/finalayze/strategies/dividend_gap.py:
```python
@dataclass(frozen=True, slots=True)
class DividendEntry:
    ex_date: datetime
    amount: float  # dividend per share in local currency

class DividendGapStrategy(BaseStrategy):
    def __init__(self, min_gap_pct=3.0, max_hold_bars=40, min_confidence=0.40):
        self._calendar: dict[str, list[DividendEntry]] = {}

    def add_dividend(self, symbol: str, entry: DividendEntry) -> None:
        self._calendar.setdefault(symbol, []).append(entry)
```

From src/finalayze/data/fetchers/tinkoff_data.py:
```python
async def _fetch_dividends_async(self, figi: str, from_date, to_date):
    """Async call to Tinkoff SDK get_dividends."""
```

Current moex_dividends.yaml format:
```yaml
SBER:
  - {ex_date: "2023-05-10", amount: 25.0}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dividend batch fetch script and expanded YAML calendar</name>
  <files>
    scripts/fetch_moex_dividends.py
    src/finalayze/strategies/presets/moex_dividends.yaml
  </files>
  <read_first>
    src/finalayze/strategies/presets/moex_dividends.yaml
    src/finalayze/data/fetchers/tinkoff_data.py
    src/finalayze/strategies/dividend_gap.py
    config/segments.py
  </read_first>
  <action>
1. Create `scripts/fetch_moex_dividends.py` -- a standalone async script that:
   - Imports TinkoffFetcher (or directly uses `t_tech.invest.AsyncClient`)
   - Collects all unique symbols from ALL ru_* equity segments in config/segments.py (exclude bond segments ru_ofz_pd, ru_ofz_pk)
   - For each symbol, fetches dividends from 2020-01-01 to 2025-12-31 via T-Invest API `get_dividends()`
   - Maps each dividend to: `{ex_date: "YYYY-MM-DD", amount: float, status: "paid"|"cancelled"|"reduced"}`
   - T-Invest API returns `dividend_type` field -- map `Regular` -> paid, `Cancelled` -> cancelled. If the API does not distinguish cancelled dividends, the script should print a warning and mark all as "paid" (manual override needed for known cancelled events like GAZP 2022).
   - Writes output to `src/finalayze/strategies/presets/moex_dividends.yaml` in the format:
     ```yaml
     # Historical MOEX dividend last-buy-dates for backtesting
     # Source: Tinkoff Invest API (get_dividends), fetched YYYY-MM-DD
     # ex_date = last buy date (Tinkoff convention); actual ex-div is next trading day
     # status: paid = dividend paid, cancelled = board recommended but shareholders rejected,
     #         reduced = paid at lower amount than initially recommended
     SBER:
       - {ex_date: "2023-05-10", amount: 25.0, status: paid}
     ```
   - Uses `FINALAYZE_TINKOFF_TOKEN` env var (loaded via dotenv)
   - Uses `target="invest-public-api.tbank.ru:443"` for AsyncClient (NOT the old tinkoff.ru domain)
   - Sets `GRPC_DNS_RESOLVER=native` env var before gRPC calls
   - Handles rate limiting: 600 req/min max, add 0.1s delay between symbol fetches
   - Prints summary: total events fetched, per-symbol counts

2. After running the script (or if T-Invest token is unavailable), manually expand `moex_dividends.yaml` with known dividend data. At minimum, ensure the following manually-curated entries exist with correct status:

   Known cancelled/reduced dividends to add:
   - GAZP 2022-06-30: amount 52.53, status: cancelled (board recommended, shareholders rejected at AGM)
   - Include all symbols from: SBER, SBERP, GAZP, LKOH, GMKN, ROSN, TATN, TATNP, NVTK, SIBN, TRNFP, CHMF, NLMK, MAGN, PLZL, MTSS, PHOR, YNDX, OZON, TCSG, CBOM, BSPB, PIKK

   The goal is 150+ events across 20+ symbols covering 2020-2025. Each entry MUST have: ex_date (string YYYY-MM-DD), amount (float), status (string: paid/cancelled/reduced).

3. Add `status` field to every existing entry in the current YAML (all current 43 entries are paid dividends, so set status: paid on each).
  </action>
  <verify>
    <automated>python3 -c "
import yaml, pathlib
data = yaml.safe_load(pathlib.Path('src/finalayze/strategies/presets/moex_dividends.yaml').read_text())
symbols = list(data.keys())
total = sum(len(v) for v in data.values())
has_status = all('status' in e for s in data.values() for e in s)
has_cancelled = any(e.get('status') == 'cancelled' for s in data.values() for e in s)
print(f'Symbols: {len(symbols)}, Events: {total}, All have status: {has_status}, Has cancelled: {has_cancelled}')
assert len(symbols) >= 20, f'Need 20+ symbols, got {len(symbols)}'
assert total >= 150, f'Need 150+ events, got {total}'
assert has_status, 'Not all entries have status field'
assert has_cancelled, 'No cancelled dividends found'
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/fetch_moex_dividends.py exists and is executable
    - moex_dividends.yaml contains 20+ top-level symbol keys
    - moex_dividends.yaml contains 150+ total dividend entries (sum of all symbols' lists)
    - Every entry in moex_dividends.yaml has `ex_date`, `amount`, and `status` fields
    - At least one entry has `status: cancelled` (GAZP 2022)
    - moex_dividends.yaml header comment explains ex_date convention and status values
  </acceptance_criteria>
  <done>Expanded dividend calendar with 150+ events, status field on all entries, cancelled dividends included</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DividendEntry status field and calendar validation tests</name>
  <files>
    src/finalayze/strategies/dividend_gap.py
    tests/unit/test_dividend_calendar.py
  </files>
  <read_first>
    src/finalayze/strategies/dividend_gap.py
    src/finalayze/strategies/presets/moex_dividends.yaml
    tests/unit/test_strategies.py
  </read_first>
  <behavior>
    - Test 1: DividendEntry with status="cancelled" is accepted and stores status field
    - Test 2: DividendGapStrategy.generate_signal skips cancelled dividends (does not generate BUY on cancelled ex_date)
    - Test 3: Calendar YAML loads correctly with status field -- all entries parse without error
    - Test 4: Calendar has at least 150 events across 20+ symbols
    - Test 5: GAZP cancelled dividend (2022, amount ~52.53) is present with status "cancelled"
  </behavior>
  <action>
1. In `src/finalayze/strategies/dividend_gap.py`:
   - Add `status: str = "paid"` field to `DividendEntry` dataclass (default "paid" for backward compatibility)
   - In `generate_signal()`, where dividends are checked against current bar's date, skip entries where `status != "paid"`. This prevents the strategy from generating a BUY signal on a cancelled dividend's ex_date.

2. Create `tests/unit/test_dividend_calendar.py`:
   - `test_dividend_entry_accepts_status`: Create DividendEntry with status="cancelled", assert status field is "cancelled"
   - `test_dividend_entry_default_status_is_paid`: Create DividendEntry without status, assert status == "paid"
   - `test_dividend_gap_skips_cancelled_dividends`: Create DividendGapStrategy, add a cancelled dividend entry, generate_signal on the ex_date should return None (not BUY)
   - `test_dividend_gap_trades_paid_dividends`: Create DividendGapStrategy, add a paid dividend entry, generate_signal on the ex_date with sufficient gap should return BUY signal
   - `test_calendar_yaml_structure`: Load moex_dividends.yaml, assert 20+ symbols, 150+ events, all entries have ex_date/amount/status
   - `test_calendar_contains_gazp_cancelled`: Load moex_dividends.yaml, find GAZP entry with status "cancelled" and amount ~52.53
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dividend_calendar.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/finalayze/strategies/dividend_gap.py DividendEntry has `status: str = "paid"` field
    - src/finalayze/strategies/dividend_gap.py generate_signal skips entries where `status != "paid"`
    - tests/unit/test_dividend_calendar.py contains `test_dividend_gap_skips_cancelled_dividends`
    - tests/unit/test_dividend_calendar.py contains `test_calendar_yaml_structure`
    - tests/unit/test_dividend_calendar.py contains `test_calendar_contains_gazp_cancelled`
    - `uv run pytest tests/unit/test_dividend_calendar.py -x` exits 0
  </acceptance_criteria>
  <done>DividendEntry has status field, cancelled dividends are skipped in signal generation, calendar structure validated by tests</done>
</task>

</tasks>

<verification>
1. `python3 -c "import yaml; d=yaml.safe_load(open('src/finalayze/strategies/presets/moex_dividends.yaml')); print(len(d), sum(len(v) for v in d.values()))"` -- shows 20+ symbols, 150+ events
2. `uv run pytest tests/unit/test_dividend_calendar.py -x -v` -- all tests pass
3. `uv run ruff check src/finalayze/strategies/dividend_gap.py scripts/fetch_moex_dividends.py` -- no lint errors
4. `grep "status: cancelled" src/finalayze/strategies/presets/moex_dividends.yaml` -- at least one match
</verification>

<success_criteria>
- moex_dividends.yaml has 150+ events across 20+ symbols with status field on every entry
- GAZP 2022 cancelled dividend is present
- DividendGapStrategy skips cancelled dividends in signal generation
- Fetch script exists for future calendar updates
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-foundation/08-03-SUMMARY.md`
</output>

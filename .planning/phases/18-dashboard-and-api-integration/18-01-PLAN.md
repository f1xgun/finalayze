---
phase: 18-dashboard-and-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/api/v1/sandbox.py
  - src/finalayze/api/v1/router.py
  - src/finalayze/main.py
  - tests/unit/test_api_sandbox.py
autonomous: true
requirements: [GATE-03]

must_haves:
  truths:
    - "GET /api/v1/sandbox/gonogo returns structured pass/fail report with per-criterion breakdown"
    - "Endpoint returns DEFER verdict when insufficient sandbox data (<5 trading days)"
    - "Endpoint requires X-API-Key authentication"
    - "Endpoint returns 503 when GoNoGoReporter is not configured"
  artifacts:
    - path: "src/finalayze/api/v1/sandbox.py"
      provides: "REST endpoint for go/no-go gate evaluation"
      exports: ["router", "set_go_no_go_reporter", "GoNoGoResponse", "CriterionResponse"]
    - path: "tests/unit/test_api_sandbox.py"
      provides: "Endpoint tests covering 200, 503, DEFER, auth scenarios"
      min_lines: 50
  key_links:
    - from: "src/finalayze/api/v1/sandbox.py"
      to: "src/finalayze/monitoring/go_no_go.py"
      via: "module-level _go_no_go_reporter singleton"
      pattern: "_go_no_go_reporter\\.evaluate"
    - from: "src/finalayze/api/v1/router.py"
      to: "src/finalayze/api/v1/sandbox.py"
      via: "include_router(sandbox_router)"
      pattern: "sandbox_router"
    - from: "src/finalayze/main.py"
      to: "src/finalayze/api/v1/sandbox.py"
      via: "set_go_no_go_reporter() in lifespan"
      pattern: "set_go_no_go_reporter"
---

<objective>
Create REST endpoint GET /api/v1/sandbox/gonogo that exposes GoNoGoReporter evaluation over HTTP with Pydantic response models, API key auth, and proper wiring in main.py lifespan.

Purpose: Enables programmatic access to sandbox gate evaluation (GATE-03) for automation and dashboard consumption.
Output: Working REST endpoint with tests, router registration, and lifespan wiring.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dashboard-and-api-integration/18-CONTEXT.md
@.planning/phases/18-dashboard-and-api-integration/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/finalayze/monitoring/go_no_go.py:
```python
class GateVerdict(StrEnum):
    PROCEED = "PROCEED"
    DEFER = "DEFER"
    ABORT = "ABORT"

@dataclass(frozen=True)
class CriterionResult:
    name: str
    passed: bool
    actual: float
    threshold: float
    unit: str
    critical: bool

@dataclass(frozen=True)
class GateReport:
    verdict: GateVerdict
    criteria: list[CriterionResult]
    sandbox_days: int
    evaluated_at: datetime
    reason: str

class GoNoGoReporter:
    def __init__(self, thresholds: GateThresholds, market_id: str = "moex") -> None: ...
    async def evaluate(self, session: AsyncSession) -> GateReport: ...
```

From src/finalayze/api/v1/auth.py:
```python
api_key_auth  # FastAPI Depends() security dependency
```

From src/finalayze/core/db.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]: ...
```

From src/finalayze/api/v1/router.py:
```python
api_router = APIRouter()
api_router.include_router(system_router)
# ... other routers
```

From src/finalayze/api/v1/system.py (pattern reference):
```python
# Module-level singleton pattern:
_health_monitor: HealthMonitor | None = None
def set_health_monitor(monitor: HealthMonitor) -> None:
    global _health_monitor
    _health_monitor = monitor
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REST endpoint and tests for /sandbox/gonogo</name>
  <files>src/finalayze/api/v1/sandbox.py, tests/unit/test_api_sandbox.py, src/finalayze/api/v1/router.py</files>
  <read_first>
    - src/finalayze/api/v1/system.py (module-level setter pattern, Pydantic response models with ConfigDict(frozen=True))
    - src/finalayze/api/v1/auth.py (api_key_auth dependency)
    - src/finalayze/api/v1/router.py (router registration pattern)
    - src/finalayze/monitoring/go_no_go.py (GoNoGoReporter.evaluate signature, GateReport/CriterionResult frozen dataclasses)
    - src/finalayze/core/db.py (get_db FastAPI dependency)
    - tests/unit/test_api_health.py (test pattern: httpx AsyncClient + ASGITransport + dependency_overrides)
  </read_first>
  <behavior>
    - Test 1: GET /sandbox/gonogo returns 200 with valid GoNoGoResponse JSON when reporter is configured and has sufficient data (mock GoNoGoReporter.evaluate returning a GateReport with PROCEED verdict and 8 criteria)
    - Test 2: GET /sandbox/gonogo returns 503 with "GoNoGoReporter not configured" when set_go_no_go_reporter was never called
    - Test 3: GET /sandbox/gonogo returns 200 with DEFER verdict and empty criteria when GoNoGoReporter returns insufficient data report
    - Test 4: GET /sandbox/gonogo without X-API-Key header returns 401/403
  </behavior>
  <action>
    1. Create `src/finalayze/api/v1/sandbox.py`:
       - `from __future__ import annotations`, TYPE_CHECKING guard for GoNoGoReporter and AsyncSession
       - Pydantic response models: `CriterionResponse(name, passed, actual, threshold, unit, critical)` and `GoNoGoResponse(verdict, criteria, sandbox_days, evaluated_at, reason)` both with `ConfigDict(frozen=True)`
       - Module-level `_go_no_go_reporter: GoNoGoReporter | None = None` with `set_go_no_go_reporter()` setter (matching system.py pattern)
       - `router = APIRouter(tags=["sandbox"])`
       - `@router.get("/sandbox/gonogo", response_model=GoNoGoResponse, dependencies=[Depends(api_key_auth)])` endpoint that:
         - Returns 503 HTTPException if `_go_no_go_reporter is None`
         - Calls `await _go_no_go_reporter.evaluate(session)` where session comes from `Depends(get_db)`
         - Converts frozen dataclass GateReport to Pydantic GoNoGoResponse field-by-field (GateReport is a frozen dataclass, NOT Pydantic -- cannot use .model_dump())
         - Uses `report.verdict.value` for string serialization of GateVerdict StrEnum
         - Uses `report.evaluated_at.isoformat()` for datetime serialization

    2. Register in `src/finalayze/api/v1/router.py`:
       - Add `from finalayze.api.v1.sandbox import router as sandbox_router`
       - Add `api_router.include_router(sandbox_router)`

    3. Create `tests/unit/test_api_sandbox.py`:
       - Follow test_api_health.py pattern: `build_test_app()` helper, httpx AsyncClient with ASGITransport
       - Mock GoNoGoReporter with AsyncMock returning a fake GateReport frozen dataclass
       - Use `app.dependency_overrides[get_db]` to inject a mock AsyncSession
       - Test all 4 behaviors listed above
       - Use constants for HTTP status codes (HTTP_200 = 200, HTTP_401 = 401, HTTP_503 = 503)
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_api_sandbox.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test cases pass
    - GoNoGoResponse matches GateReport structure with proper field mapping
    - Endpoint requires API key auth
    - 503 returned when reporter not configured
  </acceptance_criteria>
  <done>GET /sandbox/gonogo returns structured pass/fail report, requires auth, handles missing reporter and insufficient data cases</done>
</task>

<task type="auto">
  <name>Task 2: Wire GoNoGoReporter to sandbox endpoint in main.py lifespan</name>
  <files>src/finalayze/main.py</files>
  <read_first>
    - src/finalayze/main.py (existing lifespan wiring: see lines 79-94 where GoNoGoReporter is already created and wired to _bot_handler_instance -- reuse that same instance)
    - src/finalayze/api/v1/sandbox.py (set_go_no_go_reporter setter created in Task 1)
  </read_first>
  <action>
    In main.py lifespan, after the existing block that creates `go_no_go_reporter` and wires it to `_bot_handler_instance` (around line 90-92):
    1. Add import: `from finalayze.api.v1.sandbox import set_go_no_go_reporter as set_sandbox_reporter` (deferred inside the try block, matching existing pattern of deferred imports in lifespan)
    2. Call `set_sandbox_reporter(go_no_go_reporter)` right after the reporter is created
    3. Add `log.info("go_no_go_reporter_wired_to_sandbox_endpoint")`

    IMPORTANT: The GoNoGoReporter is already being instantiated at line 90. Reuse that same instance -- do NOT create a second one. The `set_sandbox_reporter` call goes right after `_bot_handler_instance._go_no_go_reporter = go_no_go_reporter` (line 91).

    Also handle the case where `_bot_handler_instance is None` but gate config exists -- the reporter should still be created and wired to the sandbox endpoint even if there's no Telegram bot.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_api_sandbox.py tests/unit/test_api_health.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - main.py wires GoNoGoReporter to sandbox endpoint via set_go_no_go_reporter
    - Same GoNoGoReporter instance used for both Telegram bot and REST endpoint
    - No import errors, existing tests still pass
  </acceptance_criteria>
  <done>GoNoGoReporter wired to sandbox REST endpoint in lifespan -- /sandbox/gonogo is functional when system starts in SANDBOX/REAL mode with gate_thresholds.yaml present</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_api_sandbox.py -x -v` -- all sandbox endpoint tests pass
- `uv run pytest tests/unit/test_api_health.py -x -v` -- existing health tests unbroken
- `uv run ruff check src/finalayze/api/v1/sandbox.py src/finalayze/main.py` -- no lint errors
- `uv run ruff format --check src/finalayze/api/v1/sandbox.py` -- formatted
</verification>

<success_criteria>
- GET /api/v1/sandbox/gonogo returns GoNoGoResponse JSON with verdict, criteria, sandbox_days, evaluated_at, reason
- Endpoint requires X-API-Key auth (returns 401/403 without)
- Returns 503 when GoNoGoReporter not configured
- Returns DEFER verdict with "Insufficient data" reason when <5 sandbox days
- GoNoGoReporter wired in main.py lifespan using existing instance
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-and-api-integration/18-01-SUMMARY.md`
</output>

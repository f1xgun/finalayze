---
phase: 52-portfolio-review-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/analysis/portfolio_review_agent.py
  - tests/unit/test_portfolio_review_agent.py
autonomous: true
requirements: [PFRA-01, PFRA-02, PFRA-03]

must_haves:
  truths:
    - "PortfolioReviewResult schema validates successfully with position summaries, concentration warnings, catalyst events"
    - "PortfolioReviewResult has no direction, confidence, or side fields — module-level assertion enforces at import time"
    - "PortfolioReviewResult does not have both symbol and market_id field names (uses ticker/market instead)"
    - "format_review_telegram() produces a structured multi-section Telegram message from a PortfolioReviewResult"
    - "build_review_prompt() constructs an LLM prompt from portfolio data dict"
    - "Code-grep test confirms zero references to BrokerRouter, place_order, generate_signal in agent module"
  artifacts:
    - path: "src/finalayze/analysis/portfolio_review_agent.py"
      provides: "PortfolioReviewResult + sub-schemas + prompt builder + Telegram formatter"
      contains: "class PortfolioReviewResult"
    - path: "tests/unit/test_portfolio_review_agent.py"
      provides: "Unit tests for schema safety, formatting, prompt building, code-grep"
      min_lines: 80
  key_links:
    - from: "src/finalayze/analysis/portfolio_review_agent.py"
      to: "pydantic BaseModel"
      via: "frozen Pydantic v2 schema"
      pattern: "model_config.*frozen.*True"
---

<objective>
Create the PortfolioReviewResult schema, prompt builder, and Telegram formatter in a new Layer 3 module, with TDD tests enforcing the advisory-only safety contract.

Purpose: Establish the schema contracts and formatting logic that Plan 02 will wire into TradingLoop. The safety-by-design contract (no trade-directive fields) is the critical PFRA-03 deliverable.
Output: `analysis/portfolio_review_agent.py` with schemas + helpers, `tests/unit/test_portfolio_review_agent.py` with safety + formatting tests.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-portfolio-review-agent/52-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/finalayze/core/schemas.py (Signal — fields to AVOID in PortfolioReviewResult):
```python
class Signal(BaseModel):
    model_config = ConfigDict(frozen=True)
    strategy_name: str
    symbol: str        # AVOID — use "ticker" instead
    market_id: str     # AVOID — use "market" instead
    segment_id: str
    direction: SignalDirection  # FORBIDDEN in PortfolioReviewResult
    confidence: float          # FORBIDDEN in PortfolioReviewResult
    features: dict[str, float]
    reasoning: str
```

From src/finalayze/execution/broker_base.py (OrderRequest — fields to AVOID):
```python
@dataclass(frozen=True)
class OrderRequest:
    symbol: str
    side: Literal["BUY", "SELL"]  # FORBIDDEN in PortfolioReviewResult
    quantity: Decimal
```

From src/finalayze/analysis/llm_client.py (parse_structured signature):
```python
class LLMClient(ABC):
    async def parse_structured(
        self,
        prompt: str,
        system: str,
        response_model: type[T],
        *,
        max_tokens: int | None = None,
    ) -> T:
```

From src/finalayze/analysis/anomaly_detector.py (Layer 3 module pattern to follow):
```python
"""Statistical anomaly detection for candle data (Layer 3)."""
from __future__ import annotations
from pydantic import BaseModel, ConfigDict

class AnomalyResult(BaseModel):
    model_config = ConfigDict(frozen=True)
    # ... fields ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PortfolioReviewResult schema + safety tests (RED then GREEN)</name>
  <files>src/finalayze/analysis/portfolio_review_agent.py, tests/unit/test_portfolio_review_agent.py</files>
  <read_first>
    - src/finalayze/analysis/anomaly_detector.py (Layer 3 module pattern)
    - src/finalayze/core/schemas.py (Signal fields to verify disjointness)
    - src/finalayze/execution/broker_base.py (OrderRequest fields to verify disjointness)
  </read_first>
  <behavior>
    - Test: PortfolioReviewResult instantiates with valid data (positions, concentration_warnings, catalyst_events, overall_assessment, risk_score, reviewed_at)
    - Test: PortfolioReviewResult is frozen (assignment raises ValidationError)
    - Test: PortfolioReviewResult has NO field named "direction", "confidence", or "side" (module-level assertion + test)
    - Test: PositionSummary uses "ticker" and "market" NOT "symbol" and "market_id"
    - Test: Code-grep for BrokerRouter, place_order, generate_signal in portfolio_review_agent.py returns zero results
    - Test: PositionSummary, ConcentrationWarning, CatalystEvent all validate with correct data
    - Test: PortfolioReviewResult with default_factory=list for optional list fields validates with minimal data
  </behavior>
  <action>
RED phase: Create `tests/unit/test_portfolio_review_agent.py` with the behavior tests above. Tests import from `analysis.portfolio_review_agent` which does not exist yet, so all tests fail.

GREEN phase: Create `src/finalayze/analysis/portfolio_review_agent.py` (Layer 3) with:

1. **Sub-schemas** (all frozen Pydantic v2 BaseModel):
   - `PositionSummary`: ticker (str), market (str), quantity (Decimal), unrealized_pnl (Decimal), pct_of_portfolio (float)
   - `ConcentrationWarning`: ticker (str), market (str), concentration_pct (float), warning_level (str)
   - `CatalystEvent`: ticker (str), event_type (str), expected_date (str)

2. **Main schema** `PortfolioReviewResult` (frozen):
   - reviewed_at (datetime), positions (list[PositionSummary], default_factory=list), concentration_warnings (list[ConcentrationWarning], default_factory=list), catalyst_events (list[CatalystEvent], default_factory=list), overall_assessment (str), risk_score (float)

3. **Module-level safety assertion** (per PFRA-03):
   ```python
   _FORBIDDEN_FIELDS = {"direction", "confidence", "side"}
   _review_fields = set(PortfolioReviewResult.model_fields)
   assert not (_review_fields & _FORBIDDEN_FIELDS), (
       f"PortfolioReviewResult has forbidden trade-directive fields: "
       f"{_review_fields & _FORBIDDEN_FIELDS}"
   )
   ```

4. **Module docstring**: `"""Advisory portfolio review agent (Layer 3). ..."""`

5. Use `from __future__ import annotations` at top.

6. **Code-grep test** uses `subprocess.run` to verify zero matches of `BrokerRouter|place_order|generate_signal` in the agent module file. Per PFRA-03 and CONTEXT.md success criterion.

IMPORTANT field naming per PFRA-03: Use `ticker`/`market` NOT `symbol`/`market_id` to be structurally distinct from Signal.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_portfolio_review_agent.py -x -v && uv run ruff check src/finalayze/analysis/portfolio_review_agent.py tests/unit/test_portfolio_review_agent.py && uv run ruff format --check src/finalayze/analysis/portfolio_review_agent.py tests/unit/test_portfolio_review_agent.py && uv run mypy src/finalayze/analysis/portfolio_review_agent.py --strict</automated>
  </verify>
  <acceptance_criteria>All schema safety tests pass. Module-level assertion runs at import time. Code-grep returns zero forbidden references. ruff + mypy clean.</acceptance_criteria>
  <done>PortfolioReviewResult schema exists with frozen sub-schemas, no trade-directive fields, module-level assertion, and all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt builder + Telegram formatter with tests</name>
  <files>src/finalayze/analysis/portfolio_review_agent.py, tests/unit/test_portfolio_review_agent.py</files>
  <read_first>
    - src/finalayze/analysis/portfolio_review_agent.py (schema from Task 1)
    - src/finalayze/core/alerts.py (TelegramAlerter._send signature)
    - .planning/phases/52-portfolio-review-agent/52-RESEARCH.md (Pattern 5: Telegram format)
  </read_first>
  <behavior>
    - Test: build_review_prompt() returns a string containing position tickers, P&L values, and equity when given a portfolio data dict
    - Test: build_review_prompt() handles empty portfolio data (no positions) without error
    - Test: format_review_telegram() returns a multi-section string with "Portfolio Review", position lines, concentration warnings, catalyst events, and assessment
    - Test: format_review_telegram() handles PortfolioReviewResult with empty lists gracefully (shows "No open positions" or similar)
    - Test: format_review_telegram() output contains specific ticker names from input data
  </behavior>
  <action>
RED phase: Add tests to `tests/unit/test_portfolio_review_agent.py` for `build_review_prompt()` and `format_review_telegram()` functions. Tests call these functions which don't exist yet.

GREEN phase: Add to `src/finalayze/analysis/portfolio_review_agent.py`:

1. **Constants**:
   - `PORTFOLIO_REVIEW_SYSTEM_PROMPT`: str constant instructing LLM to analyze portfolio and return JSON matching PortfolioReviewResult schema. Include the JSON schema in the prompt text. Emphasize: "Do not give trade directives. Focus on concentration risk, upcoming catalysts, and overall risk assessment."
   - `REVIEW_LLM_TIMEOUT = 60.0`: timeout for parse_structured call (longer than anomaly's 30s because portfolio prompts are larger)

2. **`build_review_prompt(portfolio_data: dict[str, object]) -> str`**:
   - Takes the dict returned by `_gather_portfolio_data()` in Plan 02 (keys are market_id strings, values are dicts with equity, cash, positions)
   - Formats each market's positions into a readable text block for the LLM
   - Handles empty portfolio_data gracefully (returns prompt noting "no positions")

3. **`format_review_telegram(result: PortfolioReviewResult) -> str`**:
   - Returns a structured multi-section Telegram message (NOT free-form prose, per CONTEXT.md)
   - Sections: header with date, positions summary (ticker, market, qty, PnL, % of portfolio), concentration warnings (with warning level), catalyst events (ticker + event_type + date), overall assessment, risk score
   - Handles empty lists gracefully (e.g., "No concentration warnings")
   - Uses plain text formatting (Telegram MarkdownV2 is fragile; plain text is robust)

Both functions are pure (no IO, no side effects) — easy to test.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run pytest tests/unit/test_portfolio_review_agent.py -x -v && uv run ruff check src/finalayze/analysis/portfolio_review_agent.py tests/unit/test_portfolio_review_agent.py && uv run ruff format --check src/finalayze/analysis/portfolio_review_agent.py tests/unit/test_portfolio_review_agent.py && uv run mypy src/finalayze/analysis/portfolio_review_agent.py --strict</automated>
  </verify>
  <acceptance_criteria>build_review_prompt and format_review_telegram tested and passing. Telegram output is structured with sections, not free-form prose. All prior schema tests still pass.</acceptance_criteria>
  <done>Portfolio review agent module complete: schemas + safety assertions + prompt builder + Telegram formatter. All tests green, ruff + mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> PortfolioReviewResult | Untrusted LLM JSON validated by Pydantic before use |
| Agent module -> order pipeline | MUST NOT exist -- enforced by schema design + code-grep test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Elevation | PortfolioReviewResult schema | mitigate | No direction/confidence/side fields; module-level assertion at import; code-grep test verifies zero BrokerRouter/place_order/generate_signal references |
| T-52-02 | Tampering | LLM JSON output | mitigate | Pydantic model_validate_json rejects malformed/extra fields; frozen schema prevents mutation after parse |
| T-52-03 | Spoofing | PortfolioReviewResult vs Signal | mitigate | Field names (ticker/market) structurally different from Signal (symbol/market_id); no confidence field exists |
</threat_model>

<verification>
All tests pass: `uv run pytest tests/unit/test_portfolio_review_agent.py -x -v`
Lint clean: `uv run ruff check src/finalayze/analysis/portfolio_review_agent.py`
Format clean: `uv run ruff format --check src/finalayze/analysis/portfolio_review_agent.py`
Type check clean: `uv run mypy src/finalayze/analysis/portfolio_review_agent.py --strict`
</verification>

<success_criteria>
- PortfolioReviewResult, PositionSummary, ConcentrationWarning, CatalystEvent schemas exist and are frozen
- Module-level assertion blocks any future addition of direction/confidence/side fields
- Code-grep test verifies zero BrokerRouter/place_order/generate_signal references
- build_review_prompt() and format_review_telegram() produce structured output
- All tests pass, ruff clean, mypy strict clean
</success_criteria>

<output>
After completion, create `.planning/phases/52-portfolio-review-agent/52-01-SUMMARY.md`
</output>

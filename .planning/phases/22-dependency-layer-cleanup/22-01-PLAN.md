---
phase: 22-dependency-layer-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/orchestration/__init__.py
  - src/finalayze/orchestration/trading_loop.py
  - src/finalayze/orchestration/bond_cycle.py
  - src/finalayze/orchestration/CLAUDE.md
  - src/finalayze/core/__init__.py
  - src/finalayze/api/telegram_bot.py
  - src/finalayze/api/alerts.py
  - src/finalayze/api/__init__.py
  - src/finalayze/core/CLAUDE.md
  - src/finalayze/main.py
  - src/finalayze/core/kill_switch.py
  - src/finalayze/core/layer_ledger.py
  - src/finalayze/monitoring/health_monitor.py
  - src/finalayze/monitoring/sandbox_monitor.py
  - src/finalayze/monitoring/anomaly_detector.py
  - src/finalayze/api/v1/telegram.py
  - src/finalayze/backtest/bond_engine.py
  - scripts/run_sandbox.py
autonomous: true
requirements:
  - LAYER-01
  - LAYER-02

must_haves:
  truths:
    - "trading_loop.py and bond_cycle.py are importable from finalayze.orchestration"
    - "telegram_bot.py and alerts.py are importable from finalayze.api"
    - "core/ no longer contains trading_loop.py, bond_cycle.py, telegram_bot.py, or alerts.py as actual modules"
    - "All existing imports via finalayze.core.* still resolve (backward compat re-exports)"
    - "All 2325+ tests pass without modification to test files"
  artifacts:
    - path: "src/finalayze/orchestration/__init__.py"
      provides: "New orchestration module package"
    - path: "src/finalayze/orchestration/trading_loop.py"
      provides: "TradingLoop class in correct layer"
    - path: "src/finalayze/orchestration/bond_cycle.py"
      provides: "BondCycleProcessor in correct layer"
    - path: "src/finalayze/orchestration/CLAUDE.md"
      provides: "Layer documentation for orchestration module"
    - path: "src/finalayze/core/__init__.py"
      provides: "Re-exports for backward compatibility"
  key_links:
    - from: "src/finalayze/core/__init__.py"
      to: "src/finalayze/orchestration/trading_loop.py"
      via: "re-export TradingLoop"
      pattern: "from finalayze\\.orchestration\\.trading_loop import TradingLoop"
    - from: "src/finalayze/main.py"
      to: "src/finalayze/orchestration/trading_loop.py"
      via: "deferred import"
      pattern: "from finalayze\\.orchestration\\.trading_loop import TradingLoop"
---

<objective>
Move orchestration and notification files out of core/ to their correct dependency layers.

Purpose: core/ should contain only Layer 0 types/schemas. trading_loop.py and bond_cycle.py are orchestrators (Layer 5+) that import from strategies, risk, execution. alerts.py and telegram_bot.py are notification/API layer (Layer 6). Moving them enforces the dependency layering rules and makes the architecture honest.

Output: New orchestration/ module with trading_loop.py and bond_cycle.py. alerts.py and telegram_bot.py moved to api/. Re-exports in core/__init__.py for backward compatibility so tests don't need mass updates.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dependency-layer-cleanup/22-CONTEXT.md
@src/finalayze/core/CLAUDE.md
@src/finalayze/api/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move orchestration files and create re-exports</name>
  <read_first>
    - src/finalayze/core/trading_loop.py (first 60 lines — imports and class def)
    - src/finalayze/core/bond_cycle.py (first 60 lines — imports and class def)
    - src/finalayze/core/__init__.py
    - src/finalayze/core/kill_switch.py (imports only)
  </read_first>
  <files>
    src/finalayze/orchestration/__init__.py
    src/finalayze/orchestration/trading_loop.py
    src/finalayze/orchestration/bond_cycle.py
    src/finalayze/orchestration/CLAUDE.md
    src/finalayze/core/__init__.py
  </files>
  <action>
    1. Create `src/finalayze/orchestration/` directory.

    2. **Move trading_loop.py**: Copy `src/finalayze/core/trading_loop.py` to `src/finalayze/orchestration/trading_loop.py`. In the new file, update ONLY internal cross-references:
       - `from finalayze.core.alerts import TelegramAlerter` -> `from finalayze.api.alerts import TelegramAlerter` (TYPE_CHECKING guard, line ~48)
       - `from finalayze.core.alerts import AlertPriority` -> `from finalayze.api.alerts import AlertPriority` (deferred imports at lines ~651, ~1040, ~2024)
       - `from finalayze.core.bond_cycle import BondCycleProcessor` -> `from finalayze.orchestration.bond_cycle import BondCycleProcessor` (TYPE_CHECKING guard, line ~49)
       - Keep all other imports unchanged.

    3. **Move bond_cycle.py**: Copy `src/finalayze/core/bond_cycle.py` to `src/finalayze/orchestration/bond_cycle.py`. Update:
       - `from finalayze.core.alerts import TelegramAlerter` -> `from finalayze.api.alerts import TelegramAlerter` (TYPE_CHECKING guard, line ~34)

    4. **Delete originals**: Remove `src/finalayze/core/trading_loop.py` and `src/finalayze/core/bond_cycle.py`.

    5. **Create orchestration/__init__.py**:
       ```python
       """Orchestration module — trading loop and bond cycle processors (Layer 5)."""
       from __future__ import annotations
       ```

    6. **Create orchestration/CLAUDE.md** documenting:
       - Purpose: Top-level trading orchestrators (APScheduler-based live loop, bond cycle processor)
       - Layer: Layer 5 — Orchestration. Imports from all lower layers (0-4) plus execution (L5). Injected with L6 dependencies (MetricsCollector, TelegramAlerter) via constructor.
       - Key Files: trading_loop.py (TradingLoop), bond_cycle.py (BondCycleProcessor, apply_ofz_rotation)
       - Public API: TradingLoop, BondCycleProcessor, BondCycleResult, apply_ofz_rotation

    7. **Add backward-compat re-exports in core/__init__.py**:
       ```python
       # Backward compatibility re-exports (moved to orchestration/ in Phase 22)
       from finalayze.orchestration.trading_loop import TradingLoop as TradingLoop
       from finalayze.orchestration.bond_cycle import BondCycleProcessor as BondCycleProcessor
       from finalayze.orchestration.bond_cycle import BondCycleResult as BondCycleResult
       from finalayze.orchestration.bond_cycle import apply_ofz_rotation as apply_ofz_rotation
       ```
       This ensures all `from finalayze.core.trading_loop import TradingLoop` and `from finalayze.core.bond_cycle import ...` still work across the 70+ test import sites without mass edits.

    8. **Update direct src/ imports** (these are the production code paths that should use canonical paths):
       - `src/finalayze/main.py` lines ~221, ~220: change `from finalayze.core.trading_loop` to `from finalayze.orchestration.trading_loop`
       - `src/finalayze/core/kill_switch.py` line ~28: change `from finalayze.core.trading_loop import TradingLoop` to `from finalayze.orchestration.trading_loop import TradingLoop`
       - `src/finalayze/monitoring/health_monitor.py` line ~22: change `from finalayze.core.trading_loop import TradingLoop` to `from finalayze.orchestration.trading_loop import TradingLoop`
       - `src/finalayze/backtest/bond_engine.py` line ~166: change `from finalayze.core.bond_cycle import apply_ofz_rotation` to `from finalayze.orchestration.bond_cycle import apply_ofz_rotation`
       - `scripts/run_sandbox.py` lines ~394, ~509: change `from finalayze.core.bond_cycle` and `from finalayze.core.trading_loop` to orchestration equivalents

    NOTE: Do NOT update test files. They will continue working via core/__init__.py re-exports. This minimizes blast radius.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run python -c "from finalayze.orchestration.trading_loop import TradingLoop; from finalayze.orchestration.bond_cycle import BondCycleProcessor; from finalayze.core.trading_loop import TradingLoop; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - orchestration/ directory exists with trading_loop.py, bond_cycle.py, __init__.py, CLAUDE.md
    - core/trading_loop.py and core/bond_cycle.py are deleted
    - `from finalayze.orchestration.trading_loop import TradingLoop` works
    - `from finalayze.core.trading_loop import TradingLoop` still works (via re-export)
    - All src/ production imports updated to canonical orchestration path
  </acceptance_criteria>
  <done>TradingLoop and BondCycleProcessor importable from both orchestration/ (canonical) and core/ (compat). Original files removed from core/.</done>
</task>

<task type="auto">
  <name>Task 2: Move alerts.py and telegram_bot.py to api/</name>
  <read_first>
    - src/finalayze/core/alerts.py (first 40 lines — imports and exports)
    - src/finalayze/core/telegram_bot.py (first 40 lines — imports)
    - src/finalayze/api/__init__.py
    - src/finalayze/core/layer_ledger.py (imports only)
  </read_first>
  <files>
    src/finalayze/api/alerts.py
    src/finalayze/api/telegram_bot.py
    src/finalayze/api/__init__.py
    src/finalayze/core/__init__.py
    src/finalayze/core/CLAUDE.md
  </files>
  <action>
    1. **Move alerts.py**: Copy `src/finalayze/core/alerts.py` to `src/finalayze/api/alerts.py`. No internal import changes needed (alerts.py imports from config and core.schemas which are L0-L1, valid for L6).

    2. **Move telegram_bot.py**: Copy `src/finalayze/core/telegram_bot.py` to `src/finalayze/api/telegram_bot.py`. Update:
       - `from finalayze.core.alerts import TelegramAlerter` -> `from finalayze.api.alerts import TelegramAlerter` (TYPE_CHECKING guard, line ~24)

    3. **Delete originals**: Remove `src/finalayze/core/alerts.py` and `src/finalayze/core/telegram_bot.py`.

    4. **Add backward-compat re-exports in core/__init__.py** (append to existing re-exports from Task 1):
       ```python
       # Backward compatibility re-exports — alerts (moved to api/ in Phase 22)
       from finalayze.api.alerts import AlertPriority as AlertPriority
       from finalayze.api.alerts import TelegramAlerter as TelegramAlerter
       from finalayze.api.alerts import TelegramMessageQueue as TelegramMessageQueue
       from finalayze.api.alerts import QueuedMessage as QueuedMessage
       # Backward compatibility re-exports — telegram_bot (moved to api/ in Phase 22)
       from finalayze.api.telegram_bot import TelegramBotHandler as TelegramBotHandler
       ```

    5. **Update direct src/ imports** to canonical paths:
       - `src/finalayze/main.py` lines ~220, ~514, ~515: change all `from finalayze.core.alerts` and `from finalayze.core.telegram_bot` to `from finalayze.api.alerts` and `from finalayze.api.telegram_bot`
       - `src/finalayze/monitoring/health_monitor.py` line ~21, ~129: change `from finalayze.core.alerts` to `from finalayze.api.alerts`
       - `src/finalayze/monitoring/sandbox_monitor.py` line ~19: change to `from finalayze.api.alerts`
       - `src/finalayze/monitoring/anomaly_detector.py` lines ~17, ~99: change to `from finalayze.api.alerts`
       - `src/finalayze/core/kill_switch.py` lines ~27, ~79: change to `from finalayze.api.alerts`
       - `src/finalayze/core/layer_ledger.py` line ~21: change to `from finalayze.api.alerts`
       - `src/finalayze/api/v1/telegram.py` line ~16: change to `from finalayze.api.telegram_bot`
       - `scripts/run_sandbox.py` line ~316: change to `from finalayze.api.alerts`
       - `src/finalayze/orchestration/trading_loop.py` — already updated in Task 1
       - `src/finalayze/orchestration/bond_cycle.py` — already updated in Task 1

    NOTE: Do NOT update test files. They continue working via core/__init__.py re-exports.

    6. **Update core/CLAUDE.md**: Remove trading_loop.py, bond_cycle.py, alerts.py, telegram_bot.py from Key Files and Public API. Add a note: "trading_loop.py, bond_cycle.py moved to orchestration/ (Phase 22). alerts.py, telegram_bot.py moved to api/ (Phase 22). Re-exports in __init__.py for backward compatibility."

    7. **Update api/CLAUDE.md** (if exists) or api/__init__.py: Add alerts.py and telegram_bot.py to the module documentation.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run python -c "from finalayze.api.alerts import TelegramAlerter, AlertPriority; from finalayze.api.telegram_bot import TelegramBotHandler; from finalayze.core.alerts import TelegramAlerter; from finalayze.core.telegram_bot import TelegramBotHandler; print('OK')" && uv run pytest tests/ -x -q --timeout=120 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - api/alerts.py and api/telegram_bot.py exist
    - core/alerts.py and core/telegram_bot.py are deleted
    - `from finalayze.api.alerts import TelegramAlerter` works
    - `from finalayze.core.alerts import TelegramAlerter` still works (via re-export)
    - All tests pass
    - core/CLAUDE.md updated to reflect moves
  </acceptance_criteria>
  <done>alerts.py and telegram_bot.py live in api/. core/ no longer contains L6 code. All imports resolve via re-exports. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from finalayze.orchestration.trading_loop import TradingLoop; print('OK')"` succeeds
2. `uv run python -c "from finalayze.api.alerts import TelegramAlerter; print('OK')"` succeeds
3. `ls src/finalayze/core/trading_loop.py` fails (file deleted)
4. `ls src/finalayze/core/alerts.py` fails (file deleted)
5. `uv run pytest tests/ -x -q --timeout=120` — all tests pass
6. `uv run ruff check src/finalayze/orchestration/ src/finalayze/api/alerts.py src/finalayze/api/telegram_bot.py` — clean
</verification>

<success_criteria>
- core/ contains only L0 files: schemas.py, exceptions.py, modes.py, events.py, db.py, models.py, clock.py, validation_logger.py, kill_switch.py, layer_ledger.py, bond_math.py, bond_math_quantlib.py
- orchestration/ contains trading_loop.py and bond_cycle.py
- api/ contains alerts.py and telegram_bot.py
- All 2325+ tests pass without test file modifications
</success_criteria>

<output>
After completion, create `.planning/phases/22-dependency-layer-cleanup/22-01-SUMMARY.md`
</output>

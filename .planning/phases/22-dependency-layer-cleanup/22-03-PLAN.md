---
phase: 22-dependency-layer-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finalayze/core/events.py
  - src/finalayze/api/v1/signals.py
  - src/finalayze/api/v1/trades.py
  - src/finalayze/api/v1/news.py
  - src/finalayze/api/v1/ml.py
  - tests/unit/test_events.py
autonomous: true
requirements:
  - DEAD-01
  - DEAD-02

must_haves:
  truths:
    - "STREAM_MARKET_DATA, STREAM_SIGNALS, STREAM_EXECUTION constants are removed from EventBus"
    - "STREAM_COUPONS is preserved (used by bond_discovery.py)"
    - "Stub API endpoints return 501 Not Implemented with clear message"
    - "No consumer code references the removed stream constants"
  artifacts:
    - path: "src/finalayze/core/events.py"
      provides: "EventBus with only STREAM_COUPONS"
    - path: "src/finalayze/api/v1/signals.py"
      provides: "501 response for unimplemented signals endpoint"
    - path: "src/finalayze/api/v1/trades.py"
      provides: "501 response for unimplemented trades endpoint"
    - path: "src/finalayze/api/v1/news.py"
      provides: "501 response for unimplemented news endpoint"
    - path: "src/finalayze/api/v1/ml.py"
      provides: "501 response for unimplemented ml/status endpoint"
  key_links:
    - from: "src/finalayze/data/bond_discovery.py"
      to: "src/finalayze/core/events.py"
      via: "EventBus.STREAM_COUPONS"
      pattern: "STREAM_COUPONS"
---

<objective>
Remove dead event bus streams and convert stub API endpoints to explicit 501 responses.

Purpose: DEAD-01 removes unused pub/sub infrastructure (STREAM_MARKET_DATA, STREAM_SIGNALS, STREAM_EXECUTION) that gives the false impression of an event-driven architecture. DEAD-02 makes it explicit that certain API endpoints are not yet implemented, instead of silently returning empty data.

Output: EventBus has only STREAM_COUPONS (actively used by bond_discovery). Stub endpoints return HTTP 501 with {"detail": "Not yet implemented"}.
</objective>

<execution_context>
@/Users/f1xgun/finalayze/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/finalayze/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dependency-layer-cleanup/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead event bus streams</name>
  <read_first>
    - src/finalayze/core/events.py
    - tests/unit/test_events.py (search for STREAM_MARKET_DATA, STREAM_SIGNALS, STREAM_EXECUTION)
    - tests/unit/test_bond_discovery_task1.py (search for STREAM_COUPONS — must keep)
  </read_first>
  <files>
    src/finalayze/core/events.py
    tests/unit/test_events.py
  </files>
  <action>
    1. **Remove dead stream constants from EventBus** in `src/finalayze/core/events.py`:
       - Delete: `STREAM_MARKET_DATA = "market_data"` (line ~37)
       - Delete: `STREAM_SIGNALS = "signals"` (line ~38)
       - Delete: `STREAM_EXECUTION = "execution"` (line ~39)
       - Keep: `STREAM_COUPONS = "coupons"` (line ~40) — actively used by bond_discovery.py

    2. **Also remove dead event model classes** if they are only used by the removed streams:
       - `MarketDataEvent` class (lines ~15-20) — only meaningful with STREAM_MARKET_DATA. Check if anything imports it. If not imported outside events.py, remove it.
       - `SignalEvent` class (lines ~23-31) — only meaningful with STREAM_SIGNALS. Check if anything imports it. If not imported outside events.py, remove it.
       - Keep any event classes that are imported elsewhere.

    3. **Update tests/unit/test_events.py**: Find the test that asserts `STREAM_MARKET_DATA == "market_data"`, `STREAM_SIGNALS == "signals"`, `STREAM_EXECUTION == "execution"` (around line ~201-203). Remove those assertions. Keep any test for STREAM_COUPONS.

    4. **Verify no other consumers**: Run `grep -r "STREAM_MARKET_DATA\|STREAM_SIGNALS\|STREAM_EXECUTION" src/ tests/` to confirm nothing else references these. If anything does reference them, update or remove those references too.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && ! grep -q "STREAM_MARKET_DATA\|STREAM_SIGNALS\|STREAM_EXECUTION" src/finalayze/core/events.py && grep -q "STREAM_COUPONS" src/finalayze/core/events.py && uv run pytest tests/unit/test_events.py tests/unit/test_bond_discovery_task1.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - EventBus has only STREAM_COUPONS constant
    - STREAM_MARKET_DATA, STREAM_SIGNALS, STREAM_EXECUTION are gone from events.py
    - No code anywhere references the removed constants
    - bond_discovery.py still works with STREAM_COUPONS
    - Event tests pass
  </acceptance_criteria>
  <done>Dead event bus streams removed. Only STREAM_COUPONS (active consumer: bond_discovery.py) remains.</done>
</task>

<task type="auto">
  <name>Task 2: Convert stub API endpoints to 501 Not Implemented</name>
  <read_first>
    - src/finalayze/api/v1/signals.py
    - src/finalayze/api/v1/trades.py
    - src/finalayze/api/v1/news.py
    - src/finalayze/api/v1/ml.py
  </read_first>
  <files>
    src/finalayze/api/v1/signals.py
    src/finalayze/api/v1/trades.py
    src/finalayze/api/v1/news.py
    src/finalayze/api/v1/ml.py
  </files>
  <action>
    1. **Update signals.py**: Both endpoints (`list_signals` and `strategies_performance`) should raise HTTPException(status_code=501, detail="Not yet implemented") instead of returning empty response models. Import HTTPException from fastapi. Keep the response_model declarations and Pydantic models (they document the intended API contract). The function body becomes a single `raise HTTPException(status_code=501, detail="Not yet implemented")`.

    2. **Update trades.py**: All three endpoints (`list_trades`, `trade_analytics`, `get_trade`) should raise HTTPException(status_code=501, detail="Not yet implemented"). Note: `get_trade` already raises 404, change it to 501 since the feature is not implemented at all (not just "not found").

    3. **Update news.py**: The `list_news` endpoint should raise HTTPException(status_code=501, detail="Not yet implemented").

    4. **Update ml.py**: The `ml_status` endpoint should raise HTTPException(status_code=501, detail="Not yet implemented"). Import HTTPException from fastapi.

    Pattern for each endpoint:
    ```python
    @router.get("/endpoint", response_model=SomeResponse)
    async def endpoint_name(...) -> SomeResponse:
        """Brief docstring."""
        raise HTTPException(status_code=501, detail="Not yet implemented")
    ```

    5. **Clean up unused imports**: After changing to 501, the response model classes can stay (they document intent) but remove unused ARG001 noqa comments from parameters that are no longer used (the function body is just a raise). Actually, since FastAPI still needs the parameters for OpenAPI docs, keep the parameters but they can drop the `noqa: ARG001` since linting may or may not flag them with a raise body. Check with ruff.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/finalayze && uv run python -c "
from fastapi.testclient import TestClient
from finalayze.main import app
client = TestClient(app)
for path in ['/api/v1/signals', '/api/v1/strategies/performance', '/api/v1/trades', '/api/v1/trades/analytics', '/api/v1/news', '/api/v1/ml/status']:
    r = client.get(path, headers={'X-API-Key': 'test'})
    assert r.status_code == 501, f'{path} returned {r.status_code}, expected 501'
    assert r.json()['detail'] == 'Not yet implemented', f'{path} wrong detail'
print('All stub endpoints return 501 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/signals returns 501 with {"detail": "Not yet implemented"}
    - GET /api/v1/strategies/performance returns 501
    - GET /api/v1/trades returns 501
    - GET /api/v1/trades/analytics returns 501
    - GET /api/v1/news returns 501
    - GET /api/v1/ml/status returns 501
    - Pydantic response models are preserved for API documentation
    - ruff check passes on all modified files
  </acceptance_criteria>
  <done>All stub endpoints explicitly return 501 Not Implemented instead of silently returning empty data.</done>
</task>

</tasks>

<verification>
1. `grep "STREAM_MARKET_DATA" src/finalayze/core/events.py` returns empty
2. `grep "STREAM_COUPONS" src/finalayze/core/events.py` returns match
3. `curl -H "X-API-Key: test" localhost:8000/api/v1/signals` returns 501 (if server running)
4. `uv run pytest tests/ -x -q --timeout=120` — all tests pass
5. `uv run ruff check src/finalayze/core/events.py src/finalayze/api/v1/` — clean
</verification>

<success_criteria>
- EventBus has only STREAM_COUPONS (no dead streams)
- All 6 stub endpoints return 501 Not Implemented
- All tests pass
- No dead event infrastructure remains
</success_criteria>

<output>
After completion, create `.planning/phases/22-dependency-layer-cleanup/22-03-SUMMARY.md`
</output>
